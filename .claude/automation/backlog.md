# Autonomous Backlog

Only explicit, evidence-backed work belongs here.

| Priority | Status | Task | Evidence | Loop | Approval needed |
|---|---|---|---|---|---|
| — | empty | No approved autonomous task | — | — | — |

Statuses: `ready`, `active`, `blocked`, `done`, `rejected`.
Claude may select only `ready` items. It must not create product features from speculation.
