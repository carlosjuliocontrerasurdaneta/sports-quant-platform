---
tags: [bitacora, sqp]
creada: 2026-07-08
actualizada: 2026-07-08
---

# Bitácora — 2026-07-08

## Scheduler cerrado (sesión previa)

- `DIARIO_COMPLETO.bat` restaurado desde git (`fa59ff2`) y de vuelta como **orquestador diario único**; `LOOP_DIARIO.bat` descartado definitivamente. 5 tareas en Task Scheduler, `StartWhenAvailable=True` en todas. Ver [[Arquitectura/Automatización y operación]].
- Regiones de odds ampliadas a us,us2,uk,eu,au (`52eeca7`).

## Gate de CLV por (liga, mercado) — `bc27252`

Nueva pieza de riesgo: allow-list default-deny para stake real (`src/sqp/risk/clv_gate.py` + registro `data/bets/clv_gate.json` escrito por la auditoría CLV diaria). Un mercado solo podrá llevar stake real si su CLV mediano es positivo sobre ≥30 apuestas emparejadas a cierre. Invisible mientras dure el shadow; al levantarlo, es la regla de salida POR MERCADO. Detalle en [[Conocimiento/CLV y selección adversa]]. Suite 316 → OK.

## KI-018 cerrado — `11bd999`

`_fmt_cell` del dashboard renderiza "—" en vez de "nan" para Línea sin punto (h2h). La otra mitad del issue (filtro) ya estaba resuelta desde el 07-03 (filtro Línea → Mercado). Test de regresión incluido.

## KI-017 cerrado — `7471ce4`

Test e2e de liquidación de tenis (`tests/settlement/test_settle_tennis_e2e.py`, 4 tests): flujo completo candidates → predictions → ESPN mock → grade → persistencia, con grafías divergentes, ventana de fecha ±1, idempotencia del dedup (los candidates deben llevar `generated_at`, es parte de `DEDUP_KEY`), y fallos no fatales.

**Suite final: 321 passed, ruff limpio.**

## Proceso

- Autorización durable del usuario: implementar mejoras beneficiosas directamente sin pedir aprobación; preguntar solo ante cambios de objetivos/requisitos/alcance o acciones difíciles de revertir.
- **Bóveda Obsidian construida como segundo cerebro del proyecto** (esta estructura); regla de actualización automática añadida a `CLAUDE.md`. Ver [[Metodología de documentación]].

## Estado al cierre

Shadow mode acumulando evidencia (CLV + calibración + served stream) a costo cero. Pendientes en [[Tareas]].
