---
tags: [bitacora, sqp]
creada: 2026-07-13
actualizada: 2026-07-13
---

# Bitácora — 2026-07-13

## Consolidación de skills de Claude Code + 2 skills operativos nuevos

Contexto: revisión de los 18 skills de `.claude/skills/` y 13 comandos de
`.claude/commands/`. Diagnóstico: cobertura suficiente pero con meta-skills
solapados y contradictorios entre sí, y sin skill para las dos operaciones
manuales más recurrentes del último mes (revisión/promoción de calibración y
evaluación CLV/shadow-exit).

**Eliminados (redundantes o contradictorios):**

- `elite-software-engineer`: workflow genérico exhaustivo que contradecía el
  "Control de costos" de `CLAUDE.md` y duplicaba coding-standards.
- `low-cost-mode`: duplicaba íntegramente el "Control de costos" permanente
  de `CLAUDE.md`.
- `settle-bets` (34 palabras, casi vacío): absorbido en `daily-operations`
  como sección de liquidación (SETTLE_ALL → settle_all.py, stale voids,
  auditoría acumulada, idempotencia).
- Comando `/full-system-audit`: fusionado en el skill `full-audit` como
  "Modo orquestado" (roster de 12 especialistas + salida consolidada).

**Acotados (triggers que se pisaban):**

- `sports-analytical-system`: ya NO se activa por análisis de un deporte
  concreto ("picks de béisbol") — eso lo cubren los `quant-*`, que conocen
  adapters y estado de calibración de la plataforma. Queda solo para
  análisis multi-rol explícito (tipster/handicapper/trader/arbitrajista/
  inversor), arbitraje entre books y bankroll conversacional.
- `sports-quant-platform-architect`: queda solo para decisiones de
  arquitectura/diseño (capas, límites de módulos, providers); ya no se
  activa por cambios de código rutinarios.

**Nuevos:**

- `review-calibration`: codifica el flujo train ≠ promote
  (`train_calibration.py` → staging, `promote_calibration.py` dry-run →
  decisión humana con criterios: mejora ECE/Brier OOS, preview no
  degenerado — lección del incidente isotónico 2026-06-30 —, n de
  validación). Ver [[Conocimiento/Calibración]].
- `clv-shadow-exit`: codifica la evaluación CLV repetible
  (`clv_analysis.py`/`daily_clv`, frescura ≤90 min, gate default-deny
  min_n=30, salida shadow = n≥100 y mediana>0) para que cada revisión use
  los mismos umbrales y no se relajen para forzar una salida. Ver
  [[Conocimiento/CLV y selección adversa]].

Resultado: 17 skills (antes 18) y 12 comandos (antes 13), sin triggers en
conflicto. Commit: este mismo (skills + bóveda juntos).

## Revisión integral de calibración + gate anti-inflación a extremos

Contexto: revisión por (liga, mercado) de todos los deportes con el flujo de
`review-calibration` (retrain → dry-run → decisión). Datos: 16 grupos, 8 con
muestra ≥40 graduadas (mlb h2h/spreads/totals, tenis ATP/WTA Wimbledon h2h,
wnba h2h/spreads/totals); chile, nhl y torneos ATP menores sin muestra.

**Estado por mercado (ECE OOS crudo → calibrado, veredicto):**

- `mlb_h2h`: 0.117→0.037 (iso), 23 eventos de validación — único LIVE
  (auto-promovido hoy). Preview compresivo sano (0.90→0.61): mata edges
  fantasma de favoritos, coherente con la selección adversa conocida.
- `mlb_spreads` (0.114) y `mlb_totals` (0.097): ningún calibrador mejora
  OOS sin fallar ECE o Brier → no-op (correcto).
- Tenis Wimbledon h2h: ECE crudo altísimo (ATP 0.232, WTA 0.282) —
  confirma la inadecuación del Elo; ningún candidato pasa → no-op.
- `wnba_h2h`: candidato iso pasaba ECE+Brier+monotonía (24 filas / 8
  eventos) pero mapeaba **0.80→0.99 y 0.10→0.01** — la firma exacta del
  incidente isotónico del 06-30. Solo lo frenaba el umbral de ≥15 eventos
  de la auto-promoción; con ~1 semana más de WNBA entraba a live.
- `wnba_totals`: iso plano (todo→0.48, ECE 0.28→0.025 solo por predecir la
  tasa base) — degenerado-inocuo, sigue en staging bloqueado por <15
  eventos. NO promover.
- CLV (auditoría de hoy, n=206 emparejadas frescas): mediana global +0.00%,
  batió-el-cierre 40.3%; **ningún mercado** pasa el gate (default-deny).

**Fix shipped — 4ª condición del gate OOS (`extreme_ok`):** en
`train_calibration`, un candidato se descarta si mapea algún input ≤0.90 a
≥0.95 (asimétrico a propósito: inflar crea edges fantasma porque los picks
nacen de estimada > implícita; corregir a la baja solo suprime picks y sigue
permitido — el guard simétrico inicial rechazaba correcciones legítimas
0.10→0.02). Con el gate nuevo el retrain purga el candidato wnba_h2h en
origen (`dropped: extreme`) y también marca wnba_spreads iso y wnba_totals
beta. `mlb_h2h` live intacto. TDD: 359 tests verdes, mypy y ruff limpios.

Ver [[Conocimiento/Calibración]] y [[Conocimiento/CLV y selección adversa]].

## Hallazgo: C:\Proyectos\1\sports-quant-platform es el scaffold génesis (06-12), obsoleto

Contexto: análisis solicitado de esa carpeta. A diferencia del incidente
Nc2 (linaje divergente que iba adelantado), esta copia es **estrictamente
anterior a todo**: es el andamiaje original del 2026-06-12 por la mañana.

Evidencia: sin `.git`; `data/` vacío (solo `.gitkeep`); único artefacto con
contenido es `logs/sqp.log` con corridas demo del 06-12 08:29–10:22; 57
archivos, 15 tests, v1.0.0; sin `storage/`, sin backfill ESPN, sin
calibración entrenable, sin bankroll, sin CLV, sin BATs de orquestación;
tenis "fase 3" sin implementar. Su README coincide byte a byte con el
commit génesis de producción (`efabc00`, 06-21); ningún otro archivo
coincide con blob alguno del historial → predata el repo git. Timestamps
de disco 2026-07-13 10:07 (copiada/extraída hoy). Sin secretos
(`.env.example` únicamente). Artefacto menor: directorio vacío literal
`src\sqp\{domain,providers,...}` (mkdir con llaves sin expandir).

Veredicto: **nada por delante de producción; segura de archivar o borrar**
(decisión del usuario). No crear tareas programadas contra ella. Memoria
`cdev-production-migration` actualizada.

## Limpieza de copias legacy completada (externa) + fix del MCP graphify

Al ejecutar el borrado pedido, la limpieza ya había ocurrido fuera de la
sesión (usuario): no existen ya `C:\Proyectos`, `C:\Nueva carpeta (2)`,
`C:\ZIP` (⚠ se fue con el respaldo Nc2 del 07-11 y su historial data/bets
único) ni `OneDrive\Documentos\Proyectos` completo.

Verificación post-barrida: producción `C:\dev` intacta; las 5 tareas
`SQP_*_Cdev` no referencian nada borrado; sin secretos expuestos. Daño
colateral único: con OneDrive\Proyectos se fue `.venv-graphifyy`, el exe
al que apuntaba el servidor MCP de graphify en `~/.claude.json` (pendiente
ya anotado el 2026-06-25). **Fix:** reapuntado a la instalación global
`%LOCALAPPDATA%\Programs\Python\Python314\Scripts\graphify-mcp.exe`
(existente y verificada; el hook de rebuild ya la usaba), backup en
`~/.claude.json.bak-20260713`, JSON validado. Efectivo al recargar el MCP
o reiniciar sesión.

Nota: `.claude/skills/superpowers-main` NO forma parte de esta barrida —
se eliminó deliberadamente vía merge el 2026-07-04; no restaurar.

## Revisión de skills: purga de paquetes .skill sueltos + alta de markitdown

Revisión de las dos poblaciones de skills. Las 17 instaladas en
`.claude/skills/` quedan como están (consolidación de la mañana, commit
5188bbe, sin hallazgos nuevos). Los 7 paquetes `.skill` (ZIP) aparecidos
hoy en la raíz del repo eran un ecosistema paralelo orientado a Claude.ai
web: dos colisionaban por nombre con skills instaladas pero con triggers
anchos que revertirían el estrechamiento de scope del 07-13
(sports-analytical-system, sports-quant-platform-architect); el resto era
redundante con CLAUDE.md, rules y skills existentes (chief-engineer router,
elite-software-engineer, low-cost-mode, memoria-persistente-pro).

Acción: los 7 movidos fuera del repo a `C:\Users\Richard\claude-ai-skills\`
(no se committean, regla de no generated junk). Única capacidad nueva
rescatada: **markitdown** (conversión PDF/DOCX/XLSX/PPTX→Markdown vía
Microsoft MarkItDown, solo a petición explícita), instalada como skill
del proyecto en `.claude/skills/markitdown/`. Total: 18 skills.
