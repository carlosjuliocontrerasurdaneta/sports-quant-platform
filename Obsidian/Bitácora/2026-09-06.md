# 2026-09-06

## El CI llevaba en rojo desde anoche por medio segundo

Auditoría integral. El hallazgo que más me gusta no es el más grave: es el que
explica por qué el arreglo anterior no bastó.

`FileCache.get` decide si una entrada caducó con `time.time() - st_mtime >= ttl`.
El 2026-08-05 alguien cambió `>` por `>=` porque con `ttl=0` una entrada escrita
en el mismo tick del reloj tenía edad `0.0` y no caducaba. Correcto, y
suficiente para el caso que se estaba mirando.

Lo que quedó abierto es el otro lado: **la edad también puede salir negativa**.
`time.time()` y el `mtime` de NTFS no salen del mismo reloj ni con la misma
granularidad, así que un fichero recién escrito puede llevar un sello posterior
al instante que devuelve `time.time()`. Con edad negativa **ningún** `ttl` la
caduca, ni el cero.

Lo reproduje forzando el `mtime` medio segundo por delante: edad `−0,4996`,
`get(ttl=0)` devolviendo el valor. Arreglo: `max(0.0, ...)`.

**Lo que hace interesante el caso es por qué el test no lo cazaba.** Existe un
test de borde, escrito precisamente para esto, que congela el reloj con
`monkeypatch` **y** fija el `mtime` al mismo valor. Por construcción no puede
producir una edad negativa. Es un test que cubre el borde que ya se conocía y
que, al congelar las dos magnitudes juntas, elimina exactamente la
desincronización que causa el fallo. Cubre el síntoma anterior, no el mecanismo.

Consecuencia real: el CI de `main` llevaba rojo desde el 2026-09-05T22:01. La
alarma `alerta-ci-rojo` que se añadió el 5 **sí funcionó** — el issue #1 estaba
abierto. Esta vez el control avisó; lo que faltaba era mirarlo.

## Un default-allow escondido en la única puerta que existe para denegar

`promote_calibrators` comprueba que un candidato tenga muestra OOS suficiente
antes de instalarlo en producción. El guard estaba escrito así:

```python
meta = _load_staging_meta(key)
if meta is not None:
    ...comprobar n_val_events...
```

Y `_load_staging_meta` devuelve `None` tanto si el fichero **no existe** como si
**no parsea**. Es decir: metadato ilegible → no se comprueba nada → se promueve.

Reproducido con un candidato de **un solo evento** de validación: con metadato,
rechazado; sin metadato, promovido; con metadato corrupto, promovido.

Dos cosas lo agravan. La primera es que el docstring de esa misma función dice
que es «el guard que habría dejado fuera de producción al candidato `n_val=9`
del 2026-07-02». La segunda es que la ventana no es teórica: `train_calibration`
llama a `_set_best_method` **antes** que a `_write_staging_meta`, así que una
interrupción entre las dos deja exactamente una clave staged sin metadato.

El resto del proyecto ya sabe hacer esto bien: `clv_gate` y `prediction_gate`
deniegan por defecto cuando falta el registro. Aquí `None` significaba «adelante»
en vez de «no se sabe». Ahora significa lo segundo.

## La validación OOS lleva cinco días rota y nadie lo sabía

`Get-ScheduledTaskInfo` sobre las cinco tareas `SQP_*`:

```
SQP_Validate_OOS_Cdev   last=2026-09-01 12:00   rc=0x1   next=2026-10-01
```

Falló el 1 de septiembre y no se reintenta hasta el 1 de octubre. Ninguna capa
de monitorización se enteró, y la razón es estructural: `VALIDATE_OOS.bat`
terminaba su rama `:error` con un `echo` y `exit /b 1`. **Nada en el repositorio
lee el `LastTaskResult` del Programador de tareas** — el centinela de
`run_status.py` es el único mecanismo, y sólo lo escribían los dos BAT de la
cadena diaria. `--stage` ni siquiera aceptaba otro valor que `settle` o `run`.

Es el mismo fallo que `sqp/monitoring/run_status.py` documenta en su tercera
línea: «el 2026-07-29 terminó con `LastTaskResult = 1` y el fallo estuvo 24 h
invisible». Se arregló para la cadena diaria y se dejó igual para todo lo demás.

Arreglado: seis etapas, las cuatro BAT registran y limpian la suya, y el aviso
ahora dice **qué BAT re-ejecutar**. La causa del fallo concreto sigue sin saberse
— está en `logs/validate_oos.log`, que no puedo leer.

## Lo que no vi, y lo vio Codex

A las 10:58, en mitad de mi sesión, un Codex externo reescribió
`auditoria-integral-codex.md` con una auditoría nueva del mismo commit. Tres
HIGH. El primero está en `bankroll.py`, un fichero que **yo leí y di por bueno**.

Vi `_exigir_pnl_legible`, leí su comentario largo sobre la variante silenciosa de
pandas 3.0, comprobé que lanza `LedgerIntegridadError`, y seguí. Lo que no hice
fue leer su condición de disparo: sólo rechaza el fichero cuando **no queda
ningún** PnL numérico. Si queda uno, los demás valores ilegibles pasan por
`to_numeric(errors="coerce").fillna(0.0)` y una pérdida se convierte en un
movimiento de cero. Banca 1.000 con dos pérdidas de −400: da **600** en vez de
200, y `apply_dynamic_bankroll` acepta esa cifra sin excepción.

Es literalmente lo mismo que me pasó el 2026-09-05 con `storage/lock.py`, y lo
escribí en esta bitácora ese día: *«se me escapó por aceptar una explicación
plausible sin ejercerla»*. La defensa estaba ahí, el comentario era convincente,
y no comprobé **cuándo se activa**.

La regla que saco: un guard bien comentado merece más escepticismo, no menos.
El comentario explica por qué existe; no dice qué deja pasar.

## Cerrado hoy

Los ocho confirmados de mi informe: la caché, la puerta de promoción, el
centinela de las cuatro BAT, el digest del calibrador que avisaba y cargaba
igualmente, el hook de secretos que no miraba ningún `.md` (con `Obsidian/` y
`docs/` versionados), siete textos que describían un estado que el código ya no
tiene, el `Dockerfile` que afirmaba una paridad falsa, y las diez features
pregame que se fiaban de que el llamador hubiera recortado el histórico.

Esa última no tenía fuga: los dos consumidores recortan antes y la capa está
inerte desde el 1 de septiembre. Lo que arreglé es que la garantía dejara de ser
disciplinada para ser estructural. El test que lo fija demuestra las dos mitades:
que el corte ocurre, y que sobre una lista ya recortada es un **no-op exacto** —
que es lo único que hace seguro tocar esto en producción.

Los seis de Codex quedan sin tocar: no están en la aprobación de hoy.

Las tres lecciones de hoy (11, 12 y 13) quedan en
[[Errores y lecciones/Lecciones aprendidas]].
