# 2026-09-10 — Optimización local y pricing independiente

Solicitud del operador: optimizar el repositorio y entregarlo descargable e
implementable. Se trabajó sobre `a401f0644417036f22a13d866c9a56dae2cdc324`.
No se publicaron commits, no se desplegó y no se operaron APIs ni banca.

Se vectorizó la PMF con paridad exacta sobre las pruebas comparadas; se añadieron
guardas para parámetros no finitos y rejillas inválidas. Benchmark de distribución:
6.09× MLB, 4.22× NHL, 5.80× soccer en Linux/Python 3.12.14. No es una medición
de red ni de rentabilidad. Cuatro expectativas de tests de fecha local se hicieron
portables entre zonas horarias, sin cambiar el comportamiento del dashboard.

El modo nuevo `scripts/price_independent.py` trabaja con entradas deportivas
externas explícitas: guarda el freeze antes de leer cuotas y expone win/push/loss,
medias liquidaciones, precio justo, EV y no-vig emparejado. No es la implementación
íntegra de PROMPT 191 ni un nuevo modelo entrenado. Su confianza se declara
UNVALIDATED; no asigna stake y no modifica el pipeline diario.

Validación: 1929 pruebas aprobadas, 2 omitidas; Ruff y MyPy verdes. Instalación
desde una extracción nueva: pip check, Ruff, MyPy y la misma suite aprobados.
Windows no se ejecutó: los BAT nuevos solo tienen validación de contrato.

Documentación: `IMPLEMENTACION.md`, `docs/INDEPENDENT-PRICING.md` y
`audit/optimization-20260910/`. El ZIP añade manifest de hashes y parche aplicable
al commit base. Ver [[Conocimiento/Pricing independiente - contrato 2026-09-10]].

Pendiente de evidencia externa: integración con el estado privado del operador,
ejecución Windows y validación predictiva con datos históricos as-of. Ningún
parámetro de modelo, riesgo, calibración o gate se alteró para mejorar una cifra.

---

## Sesión 2 — Auditoría integral y remediación (fases 0–5)

Auditoría completa bajo la skill `full-audit`, orquestada con cinco
especialistas (seguridad, QA, ops/BAT, sistema de Skills, cartografía). Todas
las afirmaciones de severidad alta se revalidaron a mano antes de aceptarlas; una
del agente de QA resultó **falsa** (decía que los ficheros git-pesados no llevan
`mark.slow`; llevan los tres) y se movió a falsos positivos.

**Resultado del diagnóstico**: 4 HIGH, 19 MEDIUM, 24 LOW confirmados, ninguno
CRITICAL. Detalle en `audit/latest/`; la ronda del 2026-09-08 se preservó en
`audit/audit-20260908/`.

### Dos cosas que no estaban en el guion

1. **Incidente de producción**. `SQP_Diario_Completo_Cdev` falló hoy a las
   12:00:01 (`Último resultado: 1`). Durante la auditoría escribí que este árbol
   «parece un paquete entregado, no producción»; **era incorrecto**: las 5 tareas
   del Programador apuntan aquí. Y `data/` tenía 2 ficheros (`.gitkeep`) y no
   había `.env`. El despliegue del paquete sobre el directorio de producción se
   llevó el estado no versionado, que es justo lo que `.gitignore` excluye del
   ZIP. El operador restauró su copia (~2.900 ficheros y `.env`). Ver
   [[Errores y lecciones/Desplegar un ZIP sobre produccion 2026-09-10]].

2. **Contaminación de un dataset histórico**. `data/historical/
   pitcher_confirmation_log_mlb.csv` contenía **dos filas fabricadas por la suite
   de tests** ("Game1/Game2 Home Ace", `confirmed_at_utc` 2026-08-23T02:03:09Z),
   presentes en la propia copia de respaldo del operador. Causa:
   `_attach_probable_pitchers` escribía contra el `ROOT` global y cinco tests la
   llamaban directa. Alimenta el pre-registro de timing de starters
   (`docs/research/2026-08-22-*`): evidencia fabricada entrando como válida.

### Corrección

82 ficheros modificados, 3 ficheros de test nuevos, ~40 tests añadidos. **Ni un
parámetro de riesgo, gate, calibrador, modelo o estrategia tocado.** El único
cambio sobre datos fue retirar esas dos filas, con copia de seguridad.

Lo más consecuente: `if errorlevel 1` no ve códigos de salida NEGATIVOS, así que
un crash de la liquidación (0xC000013A, documentado tres veces en los propios
BAT) se leía como éxito, seguía a generación **y limpiaba su propio centinela**.
21 comprobaciones corregidas en 9 BAT, con candado de clase y validación
conductual sobre las líneas reales. Y el informe de salud no lo producía nada
automático desde el 2026-08-29: ahora se emite en las cuatro salidas del
orquestador, incluidas las tres de error, que es cuando hace falta.

### Lo que NO se cerró

- La puerta OOS (`SQP_Validate_OOS_Cdev`, fallada desde el 2026-09-01, sin
  reintento hasta el 2026-10-01) tiene su causa raíz ya corregida en código, pero
  **el ciclo sigue abierto**: ejecutar `VALIDATE_OOS.bat` quedó bloqueado por el
  clasificador de permisos y lo debe lanzar el operador.
- Pin de las acciones de CI a SHA: requiere red; no se inventan digests.
- Estado del CI: no verificable sin `.git`.

Ninguna cifra de rentabilidad, calibración o CLV se midió ni se afirma.
