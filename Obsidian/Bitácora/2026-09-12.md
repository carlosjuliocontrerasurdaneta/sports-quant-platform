# 2026-09-12 — Graphify: MCP portable y grafo completo con capa semántica

Sin cambios en `src/`, `scripts/` ni `tests/`. Dos cambios de configuración y
una reconstrucción completa del grafo (todo en `graphify-out/`, ignorado).

## 1. El MCP deja de depender de una ruta absoluta

Ayer el servidor falló por una ruta pinneada en `~/.claude.json` que se quedó
obsoleta al mover el repo; `~/.claude.json` lista 13 ubicaciones históricas del
proyecto, así que iba a repetirse. Nuevo `.mcp.json` en la raíz, ámbito de
proyecto, sin argumento: `graphify-mcp` resuelve `graphify-out/graph.json`
relativo al cwd (documentado en `graphify-mcp --help`). Claude Code pide
aprobarlo al arrancar la siguiente sesión.

Cerrado en la misma sesión: la entrada global se retiró con `claude mcp remove
graphify -s user` (el clasificador lo había bloqueado por la mañana; al segundo
intento, con autorización expresa, pasó) y se borró
`~/.claude/skills/graphify/SKILL.md.bak`, residuo de la subida 0.9.11 → 0.9.58.
`claude mcp list` → `graphify: graphify-mcp` desde el proyecto, pendiente de
aprobación al arrancar.

## 2. Grafo completo por la skill, no por el CLI

`graphify update .` (CLI, sin LLM) había dado 7.009 nodos, todos `_origin: ast`:
trata los `.md` como código y convierte cada título de sección en nodo
(«1.1 No son ~25 cortes, son 41»). La skill `/graphify` clasifica los 213 `.md`
y yaml como documentos y los extrae semánticamente con subagentes de esta sesión
(10 chunks, modelo `sonnet`, ~1,22 M tokens en total; sin clave Gemini, así que
la sesión es el LLM). Tres chunks cayeron por límite de sesión a las 09:03 y se
recuperaron: dos ya habían escrito el fichero completo, el tercero se relanzó.

Resultado: **6.098 nodos · 12.575 aristas · 472 comunidades**, 93 % EXTRACTED /
7 % INFERRED (confianza media 0,90). Cambian 1.559 títulos markdown por 657
nodos curados: conceptos (`prediction_gate`, Value Betting, `market_shrink`),
los 47 KI, decisiones, hallazgos de auditoría con arista a la función que citan.
El guard anti-encogimiento (#479) se disparó y se forzó SOLO tras medir que los
nodos perdidos eran todos `.md`/AST; copia previa en
`graphify-out/2026-09-12/graph.pre-full-build-7009.json`.

Salud del grafo (Step 4.5, declarado, no bloqueante): 865 aristas colgantes
(840 son imports de librerías externas —`pathlib`, `pandas`, `pytest`— que no
son nodos del repo; 25 son referencias semánticas entre chunks sin destino),
431 pares con varias relaciones fusionadas por el grafo no dirigido, 5 bucles.

Etiquetas: 204 comunidades (≥10 nodos) nombradas a mano; 268 «thin» llevan el
nombre de su hub, el mismo criterio del CLI. Benchmark: 60,5× menos tokens por
consulta. `graph.html` es la vista agregada (472 comunidades) porque el grafo
supera los 5.000 nodos; `GRAPH_TREE.html` de ayer sigue siendo el detalle.

Límite honesto: la capa semántica enlaza conceptos con su documento y con los
símbolos que el texto cita, pero no hay camino `write_prediction_gate()` →
«Value Betting»; los puentes doc↔código son los que la prosa nombra.

## ¿`graphify update .` destruye la capa semántica? Medido: no, pero re-etiqueta

Con copia previa: `graphify update .` CONSERVA la capa semántica (787 nodos semánticos, 0 perdidos, 0 títulos AST de `.md` reintroducidos, +3 aristas de `.mcp.json`) pero RE-CLUSTERIZA (477 comunidades frente a 472) y renombra por hub: solo 147 de 472 etiquetas curadas sobreviven. Tras un `update .`, restaurar etiquetas e informe desde `graphify-out/2026-09-12/` o asumir nombres por hub.
El grafo curado se restauró desde la copia y se regeneró `graph.html`.

## Validación

- `graphify explain`, `query`, `path` y el MCP (`graph_stats`) responden sobre
  el grafo nuevo.
- `tests/test_claude_system_contract.py`: 20 passed.
- `.mcp.json`, `graph.json`, `manifest.json`: JSON válidos; manifest con 534
  ficheros sellados, 0 pendientes de re-extracción.

Ver [[Bitácora/2026-09-11]].
