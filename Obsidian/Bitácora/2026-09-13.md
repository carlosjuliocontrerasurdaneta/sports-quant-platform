# 2026-09-13 — Auditoría integral (fases 0–3) y remediación (fases 4–5)

Skill `full-audit` sobre el árbol de producción, seguida de `audit-remediation`
con autorización expresa del operador para «todos los hallazgos confirmados y
las mejoras demostradas». Informe en `audit/latest/` (la ronda del 09-10 se
copió íntegra a `audit/audit-20260910/`). Suite de partida: 1741 + 224 passed,
1 skipped; `ruff` y `mypy` limpios.

## Lo que la auditoría encontró (resumen; detalle en `audit/latest/FINDINGS.md`)

- **AUD-HIGH-001** — Producción era un árbol plano sin `.git`, divergente de
  `main@a401f06` (GitHub, público, CI verde): 94 ficheros distintos y 41 sólo
  locales (paquete de pricing independiente + remediación del 09-10). El CI
  validaba otro código; los guards basados en Git (árbol limpio, revisión
  cruzada, `_targets --with-git`) fallaban abiertos.
- **AUD-HIGH-002** — Sin run el 07, 08, 10 y 11 de septiembre (4 de 7 días), en
  silencio: tareas «Solo interactivo» (decisión del operador, B-07) y
  `open_dashboard.ps1` se omitía justo cuando el reporte no era de hoy.
- **AUD-MED-001** — Revalidación pre-partido ciega tras las 00:00Z (2/115
  revalidados en eventos del día UTC siguiente frente a 195/369 del mismo día).
- **AUD-MED-002** — Líneas asiáticas de cuarto graduadas como enteras (1 fila
  del ledger mal graduada; 212 filas del stream en la ventana del gate).
- **AUD-MED-003 / 004** — El ledger sólo graduaba la última vista de
  `candidates_*`: 150 de 730 unidades listadas (20,5 %) nunca recibieron
  veredicto — 132 por refresco antes del partido, 18 por sobrescritura tras un
  fallo de liquidación que el guard M2 no veía (contaba sólo `stake > 0`).
- **AUD-MED-005** — Un fallo en una liga abortaba la lista diaria de todas.
- **AUD-MED-006** — La memoria registraba `PREDICTION_GATE_MIN_N = 100` como
  vigente; el código, el pre-registro y el registro vivo dicen 300.
- **AUD-MED-007** — Los hooks se armaban con comandos Bash de sólo lectura.
- LOW: `predictions_*` huérfanos sin poda; artefactos sin retención; carga del
  histórico completo de cuotas para un término inerte; residuos documentales.

## Lo que se corrigió (detalle en `audit/latest/CHANGES.md`)

Ver `CHANGES.md` para el diff resumido por ID. Ningún parámetro de riesgo,
`pick_mode`, `shadow_mode`, calibrador ni umbral de gate cambió. Los dos cambios
de contrato del ledger (`half_win`/`half_loss` y picks `superseded`) pasaron por
revisión independiente en el escalón `fable` antes de cerrarse.

**Repositorio reconstituido en el propio directorio de producción** (`git init`
+ `fetch` + `read-tree origin/main`, sin tocar el árbol de trabajo) y commit en
la rama `prod/remediacion-20260913`. `data/`, `logs/` y `.env` siguen ignorados.

## Cierre de la sesión («elige la mejor opción y aplícala»)

- CI del PR #3 verde en las 5 patas (3.11–3.14 + Windows); el **merge** lo
  bloquea el clasificador de permisos de la sesión → queda para el operador.
- `VALIDATE_OOS.bat` ejecutado: **exit 0** (B-01 cerrado tras 12 días en rojo);
  `audit/model_vs_market_20260913.md` generado (mercado mejor en Brier agregado,
  IC95 [+0,0088, +0,0164]; ningún corte con ventaja). Sin cuota de API.
- `wnba_totals_calibration_iso.joblib` retirado a `data/models/retired/`.
- MCP `graphify` habilitado en `.claude/settings.local.json` (coherente con
  `.claude/CLAUDE.md`).
- Decisión registrada: el ledger histórico NO se regradúa (pnl 0 en todo lo
  afectado; evidencia intacta en el stream servido).

## Cierre definitivo

- PR #3 **mergeado** (`c2cc9f1`); `main` es producción (`git switch main`, árbol
  idéntico). Para ello el operador añadió `Bash(gh pr merge *)` a
  `settings.json`; el pegado rompió el JSON dos veces (la clave `allow`) y con
  JSON inválido Claude Code ignora TODO el fichero, hooks y `deny` incluidos:
  reparado y commiteado (`d87d547`).
- Las 4 tareas del pipeline en **S4U + WakeToRun** vía
  `scripts/set_tasks_unattended.ps1`: S4U exige proceso elevado incluso siendo
  administrador (`Acceso denegado` desde consola normal), así que el script se
  auto-eleva con UAC. Revisión cruzada de Codex atendida: un `Set-ScheduledTask`
  denegado ya no termina en verde. Primera ejecución desatendida real: captura
  de las 18:30.
