---
tags: [bitacora, skills, auditoria, sqp]
creada: 2026-09-16
actualizada: 2026-09-16
---

# 2026-09-16 — Corrección de prompts y skills

Autorización: el usuario pidió corregir los cinco defectos de la revisión precedente.

- `.claude/skills/sports-analytical-system/SKILL.md`: stake mediante la función canónica y parámetros efectivos, retorno de arbitraje sobre capital y normalización de moneyline para dos o tres resultados.
- `audits/prompts/`: severidad por impacto conforme a `AGENTS.md` en los cuatro prompts afectados; la puntuación de riesgo queda separada. Verificación con precedencia explícita de estados y bloqueo por P0/P1 originales o introducidos.
- `tests/test_analytical_skill_examples.py`: ejecuta los ejemplos documentados para contrastar límites, overrides y retornos de ambos resultados.

Validación:

- `python -B -m pytest -q -p no:cacheprovider --basetemp=.codex-tmp/pytest-prompts-fix tests/test_analytical_skill_examples.py tests/test_claude_system_contract.py`: 30 passed, salida 0.
- `python -B -m ruff check --no-cache tests/test_analytical_skill_examples.py`: satisfactorio, salida 0.
- `quick_validate.py` sobre la skill: satisfactorio con `python -X utf8 -B`. El primer intento sin UTF-8 falló por el encoding cp1252 del entorno, no por el contenido.
- Revisión estática de los seis escenarios de decisión del prompt y del caso CRITICAL con puntuación de riesgo 68.8.

Sin cambios a configuración de riesgo, modelos ni operación productiva. No se ejecutó el pipeline ni la suite completa: el alcance es documentación de control y sus ejemplos. Sin commit.

Notas actualizadas: [[Tareas]], [[Errores y lecciones/Errores detectados y soluciones]] y [[Bitácora]].

## Corrección adicional de loops

Tras revisar los 26 archivos de `.claude/loops`, el usuario autorizó corregir
los cuatro hallazgos adicionales:

- `audit.md`: autoriza preservar la ronda anterior en su histórico y exige verificar la copia antes de escribir en `latest`.
- `quant/01` y `STATES.md`: diferencian omisiones por cuota con salida cero de fallos del proveedor con salida distinta de cero; los últimos son `BLOCKED`.
- `quant/03`: acepta una cohorte vacía verificada sin inventar archivos de liquidación; incluye comprobación de pendientes servidos y archivados.
- `quant/09` y `quant/10`: exigen comparar los artefactos congelados correctos, con identidad y hashes; los scripts de ratings/simulación no acreditan un calibrador. Si falta un evaluador adecuado, se declara `BLOCKED`.

Validación: `python -B -m pytest -q -p no:cacheprovider --basetemp=.codex-tmp/pytest-loops-fix tests/test_claude_system_contract.py tests/test_analytical_skill_examples.py`: **30 passed**, salida 0. `git diff --check`: salida 0. Revisión estática de los contratos y escenarios de archivo previo, fallo parcial, cohorte vacía y candidato no consumido por el evaluador. Estas pruebas no ejecutan los workflows operacionales ni prueban el comportamiento de un asistente siguiendo el texto.

Cambios solo documentales en esta ampliación; sin nuevas pruebas basadas en coincidencias de redacción, sin tocar scripts operativos, sin llamadas a proveedores ni promoción de modelos. Sin commit.

## Rediseño integral de instrucciones

Autorizado por el usuario tras la evaluación de duplicaciones. Implementado:

- `.claude/automation/audit-workflow.md` centraliza fases, evidencia, IDs, severidad, autorización, estados y entregables. Nuevas rondas en `audit/latest/`; informes legacy en `audits/` conservados y aceptados por fuente explícita con sus IDs.
- Los cinco prompts se generan desde ese contrato, manteniendo nombres y contenido autónomo. Las skills de auditoría son entradas acotadas; tres referencias antiguas permanecen como enlaces compatibles.
- `model-routing.json` conserva sus prioridades/modelos. Sus tres tablas de consumidores se generan en vez de mantenerse por separado.
- `loop-guardrails.md` mantiene una sola fuente de reglas comunes, expandidas en cada loop para no perderlas en cargas aisladas.
- `mlb-pipeline` es alias de la inspección estructural por liga de `daily-operations`. Se conservan las skills delgadas y los punteros de memoria; no se demostró que pudieran borrarse sin afectar consumidores externos.
- Guía de mantenimiento: `.claude/automation/INSTRUCTIONS.md`. Generador explícito `scripts/sync_agent_instructions.py --write`; modo de comprobación `--check` sin escrituras. El generador no modifica informes históricos ni modelos/datos.

Medición de Markdown en las tres carpetas revisadas: **6.542 → 3.809 líneas**.
Las tres fuentes/guía nuevas añaden 342 líneas: **4.151 en total**, reducción neta
de 2.391 líneas (36,5 %) para ese perímetro documental ampliado. Esta cifra
excluye código/tests del generador y no mide tokens de ejecución ni latencia.
Los archivos de compatibilidad se conservan deliberadamente.

Validaciones:

- `python -B -m pytest -q -p no:cacheprovider --basetemp=.codex-tmp/pytest-instruction-redesign-final tests/test_agent_instruction_sync.py tests/test_claude_system_contract.py tests/test_claude_model_routing.py tests/test_analytical_skill_examples.py`: **83 passed**, salida 0.
- Ruff sobre generador y tests afectados: satisfactorio, salida 0.
- `python -B scripts/sync_agent_instructions.py --check`: sincronizado, salida 0.
- `python -B scripts/validate_claude_model_routing.py`: OK, salida 0.
- Pruebas aisladas: propagación común y por fase, check sin escritura, idempotencia, rutas inválidas sin escrituras parciales, sincronización del routing, guardarraíles autónomos, links y metadatos. Históricos simulados permanecen idénticos.
- La primera pasada detectó un test de routing ligado a frases retiradas; se reemplazó por la comprobación de precedencia del clasificador real y su vista generada, no se eliminó la garantía.
- El validador genérico de skills validó cuatro entradas; rechaza el campo preexistente `argument-hint` de `audit-remediation`. Se conserva para compatibilidad del consumidor Claude. Los tests locales validan YAML, nombre, descripción y enlaces de las skills.

No se ejecutaron auditorías operacionales, migraciones de informes reales ni llamadas externas. No se modificaron prioridades/modelos del routing, producción ni configuración de riesgo. Sin commit.
