---
tags: [bitacora, operaciones, validacion-oos, auditoria, remediacion, sqp]
creada: 2026-09-17
actualizada: 2026-09-17
---

# 2026-09-17 — `SQP_Validate_OOS_Cdev` lanzada a mano: rc=0

Por orden del operador se lanzó la tarea programada (`Start-ScheduledTask`, 00:00:27) para sustituir el `LastTaskResult=1` rancio del 01/09 (KI-034). Terminó a las ~00:19 con **resultado 0**; el Programador ya muestra `Ready 0`. Es la primera ejecución de esta tarea bajo S4U + WakeToRun.

**Validación OOS** (`scripts/validate_oos.py`, parámetros congelados en train, ROI realizado en test): **33 ligas validadas de 34; 0 sin cuotas, 1 sin resultados** (`frauen_bundesliga`, liga retirada de la configuración: sus cuotas siguen en `data/odds` y el guard lo trata como benigno), **0 con error**. 31 ligas con apuestas graduadas en test. Frozen_train supera a full_history en 4 ligas (MLS +27,9 % vs −6,1 %; NCAAF −1,9 vs −14,2; LALIGA −33,2 vs −54,1; EPL +39,8 vs +35,0) y queda por debajo en 5 (MLB −5,4 vs −0,5 con n=711; NHL −12,5 vs −2,0 con n=134; CHILE, LIGUE1, BUNDESLIGA n=1); el resto empata porque los parámetros coinciden. No hay señal de overfit sistemático del ajuste, pero tampoco de que aporte: los dos cortes grandes (MLB, NHL) van mejor con la historia completa. ROI OOS es un backtest sobre un único snapshot pregame, nunca promesa de beneficio.

**Marcador modelo vs mercado** (`audit/model_vs_market_20260917.md`, best-effort): n=10.340 filas / 1.888 eventos; Brier modelo 0,2435 vs mercado 0,2300 (diff +0,0134, IC95 [+0,0097, +0,0174]) y calibrado 0,2340 vs 0,2300 (diff +0,0040, IC95 [+0,0023, +0,0057]): **mercado mejor** en ambos, igual que el 13/09. Contraste de selección: ROI donde el modelo apuesta −11,5 % (n=2.324) vs −5,0 % en el resto (n=8.016); delta −6,45 %, IC95 [−13,2 %, +0,2 %] — el IC roza el cero, no lo excluye (el 13/09: [−13,1 %, +1,2 %]). Consistente con [[Bitácora/2026-08-25]] (escalera de `min_edge` invertida) y con el feed público cerrado.

Avisos del roi_engine: emparejamientos ambiguos omitidos en MLB (series de días consecutivos con dos eventos de cuotas a la misma distancia y resultado solo con día) — es AUD-002, abierto. Centinela `logs/run_status/` limpio. No se cambió ningún gate ni parámetro.

Ver [[Tareas]] y [[Bitácora/2026-09-16]].


## Auditoría integral (ronda `audit-2026-09-16`) y remediación autorizada

Diagnóstico Claude sobre `44e48f2` + working tree (clon `C:\dev\6`), un solo auditor (OpenAI dejó sólo `reproduce.py`; sus 3 hipótesis se revalidaron: 2 confirmadas, 1 reclasificada como decisión registrada). **7 confirmados: HIGH 1, MEDIUM 4, LOW 2; P0 0; P1 2.** Entregables en `audit/latest/` (`claude/REPORT.md`, `FINDINGS.md`, `BACKLOG.md`, `CHANGES.md`, `VALIDATION.md`, `STATUS.md`, `MANIFEST.json`).

- **AUD-001 (HIGH)** `poisson_match_probs` trataba las líneas asiáticas de cuarto como enteras mientras `settle._grade` liquida a medias desde AUD-MED-002: 7–13 pp de desviación reproducida y signo del EV invertido (Under 2,25: 0,544 vs 0,477). 574 de 26.802 filas del stream graduado. Corregido acumulando las dos líneas adyacentes y sirviendo la probabilidad de decisión; revisión escalada en `fable` (clase parámetro de modelo): 0 defectos, barrido de 18.432 combinaciones sin excepción. Hallazgo adyacente sin ticket: mismo patrón en `normal_margin_probs` (NBA/NFL) con Δ ≤ 0,25 pp.
- **AUD-002 (MEDIUM)** tres definiciones de ROI realizado tras las medias (`_summarize`/dashboard duplicaban el ROI: 1,50 vs 0,75). Una sola definición en `settle.realized_roi_parts` para los cuatro consumidores; `hit_rate` sigue sobre win/loss.
- **AUD-003 (MEDIUM)** `44e48f2` dependía de `.claude/automation/audit-workflow.md` sin trackear y `72d07d8` rompía `test_tennis_params`: checkout limpio con 2 failed + 5 errors. Commiteado el trabajo coherente del 16/09 (`f93bdc1`), remediación (`4f06b4f`), merge con `origin/main` (`31cfdb0`, 3 conflictos Obsidian resueltos conservando ambos lados) y push. **Corrección**: producción es `C:\dev\3` (`a2ee66c`, limpio), no este clon; recibirá los cambios con `git pull` (operador).
- **AUD-004 (MEDIUM)** residuo `.codex-tmp/pytest/openai-20260916-retry` con ACL ilegible rompía el comando canónico de pytest; mitigado renombrando el padre (`pytest.bloqueado-20260916`); el borrado exige privilegios.
- **AUD-005 (MEDIUM)** `execution.books` documentado como activable y nunca consumido (decisión `9dfb4cc`): yaml y `Settings` lo declaran sin cablear y `Settings.load` avisa; cablearlo es decisión del operador.
- **AUD-006 (LOW)** `_targets.py --with-git` armaba el centinela de tests ante cualquier `cat` con árbol sucio; la red de git sólo se consulta si el comando escribe.
- **AUD-007 (LOW)** `feature_shadow.train` huellaba `--data-root`; ahora huella el código como `load_protocol`.

Validación: suite rápida 1958 tests exit 0, ruff 0, mypy 0; CI run 35224249563 lanzado con el push (resultado en `audit/latest/STATUS.md`). Pendientes: verificación independiente de la ronda; `git pull` en `C:\dev\3`; borrado con privilegios del residuo; decisión sobre el line shopping. La corrección de AUD-001 cambia la probabilidad servida de ~2 % de las líneas (spreads de fútbol): medir en la auditoría diaria.

Ver [[Tareas]] y [[Bitácora/2026-09-16]].

**Cierre (misma sesión):** pull en producción `C:\dev\3` (hoy en `953cf4f`); residuo de AUD-004 eliminado con elevación UAC; verificación independiente en `fable`: **APTO CON PENDIENTES** (AUD-005 mitigado; hallazgo Normal recuantificado a 0,32 pp; CL-02 cerrado). Pendientes: decisión sobre `execution.books`, B-02, ticket de `normal_margin_probs`.

## Run diario de las 12:00 y regresión del dashboard

`SQP_Diario_Completo_Cdev` terminó con **rc=0** (12:00:01 → ~12:10; `pipeline_health.json` OK a las 15:10Z, sin centinelas, 13 ficheros de candidates y `report_latest.html` regenerados): primer run correcto desde el 14/09 y el primero que sirve las líneas de cuarto con la probabilidad coherente con la liquidación (AUD-001).

El operador detectó en el dashboard que la columna **Partido** mostraba un hash de 32 caracteres (el `event_id`) en vez de «Visitante @ Local». Causa: regresión de AUD-MED-001 (2026-09-10) — al ganar `BetCandidate` las columnas `home`/`away`, el `merge` de `load_all_candidates` con `predictions_<liga>.csv` (que también las trae) las renombraba a `home_x`/`home_y` y `match_label` caía al identificador; afectaba a las 182 filas de la tabla de picks. Corregido en `5f344be` (sufijo sólo en la copia de predictions, usada como relleno de huecos), test discriminante en `tests/test_reports.py` (fallaba antes, pasa después), 35 tests de informes verdes, ruff y mypy limpios; producción en `5f344be` y dashboard regenerado desde los ficheros existentes: 0 hashes. La auditoría integral de la mañana tenía el dashboard como REVISADA_PARCIALMENTE y no lo cazó: queda como lección para la matriz de cobertura (renderizar el HTML y mirar una fila, no solo leer el código).
