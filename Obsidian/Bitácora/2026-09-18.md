---
tags: [bitacora, auditoria, remediacion, calibracion, gates, hooks, doctor, codex, modelos, tablero, skills, sqp]
creada: 2026-09-18
actualizada: 2026-09-18
---

# 2026-09-18 — Auditoría integral (ronda `audit-2026-09-18`) con dos auditores y remediación autorizada

Ronda inicializada por el coordinador a las 00:04 sobre `5952164` (`main`,
sincronizado, CI verde). **Dos auditores independientes**: OpenAI
(`audit/latest/openai/REPORT.md`, 00:17) y Claude (`claude/REPORT.md`, 08:30–
09:30), sin leerse hasta fijar conclusiones. Conjuntos disjuntos (3 + 4);
consolidados en `audit/latest/FINDINGS.md`: **7 confirmados, MEDIUM 4 / LOW 3,
P0/P1 ninguno**. Línea base: suite completa 2191 passed / 1 skipped, ruff 0,
mypy 0; estado de controles comprobado (5 tareas rc 0x0, gate 0/48, 11
pausas de degradación, 4 calibradores sin defecto estructural).

- **AUD-001 (OpenAI, MEDIUM)** `scripts/gate_status.py` evaluaba OTRA regla
  (aciertos vs 0,5 sobre `pick_history`) y anunciaba «PASAN EL GATE» para
  cortes que el gate real deniega. Reescrito: veredicto persistido + progreso
  con `evaluate_markets` sobre el stream graduado.
- **AUD-002 (OpenAI, MEDIUM, ampliado)** un JSON válido con raíz no objeto
  rompía el default-deny (`load_prediction_gate`) y, en `degradation.py`, el
  fallback de `run_all` lo llamaba fuera del `try`: abortaba el run entero.
  Raíz comprobada en ambos lectores; fallback que nunca lanza.
- **AUD-003 (Claude, MEDIUM)** el registro live de calibración contenía la
  clave sandbox `mlb_h2h_pergame` (promoción completa del 23/08): inerte para
  el pipeline (`mlb_h2h` en crudo) pero contada como calibrador vivo por
  health, dashboard y [[Tareas]]. Demovida con `promote_calibration.py
  --demote` (rastro en `promotion_log.csv`); la promoción rechaza claves
  sandbox y las demueve en la sincronización completa; health/dashboard las
  ignoran y las señalan. **Decisión: demover, no adoptar** — cero cambio en lo
  servido; adoptar bajo `mlb_h2h` sigue siendo la tarea abierta.
- **AUD-004 (Claude, MEDIUM)** historial del Programador de tareas
  deshabilitado: los días sin run 11, 15 y 16/09 no tienen causa
  diagnosticable. `health_check` expone ahora `LastRunTime`/`LastTaskResult`
  de las 5 tareas y avisa (WARN) mientras el historial siga apagado.
  **Pendiente del operador (consola elevada):**
  `wevtutil sl Microsoft-Windows-TaskScheduler/Operational /e:true`.
- **AUD-005 (OpenAI, LOW)** el log del run anunciaba habilitados desde la
  tabla pre-pestillo; ahora lee el registro escrito (`gate_allowed_markets`).
- **AUD-006 (Claude, LOW)** la captura de cierre pedía 3 mercados en tenis (15
  créditos vs 5): regla única `markets_for_family`.
- **AUD-007 (Claude, LOW)** `check-secrets.sh` marcaba código citado como
  secreto en cada comando (asignación reflexiva + CRLF escapado en un
  EVIDENCE.json); excluidos `audit/`, `audits/`; `ASSIGNMENT` endurecido.

Observaciones sin acción: gate en 48/50 cortes (6 de tenis); cobertura de la
captura de cierre 0–28/48 por día (máquina apagada de noche; 55,8 % de
liquidadas sin cierre); marcador `.tests-pending` entre sesiones (subconjunto
`not slow` 489,8 s vs timeout 600 s); B-02 heredado.

Validación y estado por ID: `audit/latest/{CHANGES,VALIDATION,STATUS}.md`.
Commits `c56fdf5`…`6beaf5e` + `d06bf23` (corrección de la exclusión `audit/`
señalada por Codex al cierre: `src/sqp/audit/` sigue escaneado) en `main`,
CI verde. Suite completa 2251 passed / 1 skipped.
Ver [[Tareas]], [[Bitácora/2026-09-17]] y [[Conocimiento/Calibración]].

---

## Sesión de la tarde — `/doctor` del harness, estado de las integraciones y timeout del candado de despacho

**`/doctor` (solo ficheros de usuario, nada versionado):** instalación nativa
2.1.277 al día, 6 JSON válidos, 27 agentes y 56 skills con frontmatter
correcto. Limpieza aplicada en `~/.claude/settings.json` y `~/.claude.json`:
21 skills sin uso desactivadas (20 sincronizadas desde claude.ai + `hf-cli`),
plugins `cowork-plugin-management@synced` y `skill-creator` desactivados, MCP
`github` desactivado en este proyecto (0 usos, token rechazado con 401),
`permissions.defaultMode: auto` y **corrección de `autoMode.environment`, que
seguía declarando `C:\dev\6` como repo de confianza** (árbol borrado; causa de
11 bloqueos del clasificador sobre `cd C:/dev/3/...`). Sin reglas `allow`
nuevas: las 130 denegaciones medidas eran rutas protegidas por `deny`
(`logs/`, `data/`, `.env`, `git stash`) o comandos compuestos.

**Integración Claude↔Codex, medida en 50 sesiones:** la Forma 1
(`mcp__codex__codex`) está **muerta** — Codex CLI 0.154.0 retiró el
subcomando `mcp-server` y `~/.claude.json` sigue lanzándolo (→ KI-055). La
Forma 2 (`/cross-review`) pasa sus 164 tests. La Forma 3 (centinela) se
dispara pero no completa una revisión en `C:\dev\3` desde el 08/09: 27 fallos
«Not inside a trusted directory» (11–13/09, antes de que el árbol fuera repo
Git) y 4 por límite de uso de la cuenta (13 y 17/09); ante el fallo avisa, no
bloquea y repone el centinela, como está diseñado. Vía no documentada: la
puerta Stop del plugin `codex` (`stopReviewGate: true`) revisa **todos** los
turnos (21 hoy, todos `ALLOW`, 22–109 s cada uno) y compite por la cuota con el
centinela.

**Selección de modelos (`MODEL_ROUTING.md`), medida:** 73 despachos `Agent`
en la ventana — `sonnet` 27, `fable` 19, `opus` 10, sin `model` 17, de los que
13 son del 01/09 (antes del hook). Desde entonces el hook
`require-dispatch-model.sh` bloqueó 3 omisiones y **dejó pasar 1 (11/09) por
agotar su timeout** (15 s; tardó 18,9 s por contención del host; falla abierto
por diseño). `/route-task` no se ha invocado nunca. Techo `fable` alcanzable
(2.1.277 ≥ 2.1.255, `ANTHROPIC_DEFAULT_FABLE_MODEL` sin definir).

**Cambio versionado:** `.claude/settings.json` → timeout del hook
`PreToolUse:Agent` 15 → 30 s (único cambio en el fichero; 63 tests de contrato
y routing en verde).

Avisos sin acción: hooks `Stop` del repo con mediana 16,8 s y 12 timeouts a
600 s (`run-tests-on-stop.sh`); `SessionStart` del plugin codex (timeout 5 s)
expira en 9 de 34 arranques. Ver [[Bitácora/2026-09-17]] y `KI-055`.

### Tablero: el panel visible al cargar era el recorte, no la lista completa

Reportado por el operador: «Picks del día» mostraba 10 filas al abrir y 136 al
volver desde «Historial». Causa: `fae6cdc` (05/09) quitó la pestaña `picks` y
renombró `todos` a «Picks del Dia», pero dejó `class="panel active"` en el
panel viejo `#picks` (candidatos recortados por `min_edge`, horizonte 7 días).
Al cargar se veía ese recorte bajo un rótulo que prometía la lista completa;
el manejador de pestañas, al volver, activaba el `#todos` real. Es justo la
vista que la regla fundamental prohíbe como primaria. Corregido en
`src/sqp/audit/html_report.py` (el `active` inicial pasa a `#todos`) con test
discriminante `test_el_panel_inicial_es_el_de_la_pestana_activa` (falla en
HEAD~: `pestana activa 'todos' pero paneles activos ['picks']`). 60 tests de
tablero en verde; `report_latest.html` regenerado desde las predicciones
guardadas (a las 21:39 locales: 28 caras de hoy en la lista completa, 168 en
el recorte vigente a 7 días).

### Adelgazar el sistema de instrucciones: una capa menos (`6fbf201`…`495d2e6`)

Pregunta del operador («¿demasiadas skills, loops y scripts?») respondida con
medición y ejecutada con su «Sí, hazlo». Antes: 34 skills (16 nunca invocadas,
15 de ellas un puntero de una línea a un loop), 17 comandos (9 con 0 usos de
por vida), 26 loops, 27 agentes (14 sin un despacho en 73), 65 scripts (37 en
el pipeline, 28 solo citados por docs/tests/.claude, 0 huérfanos), 7.955
líneas de Markdown en `.claude/`. Cuatro pasos, cada uno con commit y suite:

1. **Comandos** (`6fbf201`): retirados los 9 nunca usados; `route-task` queda
   (vía consultiva que nombra `MODEL_ROUTING.md`).
2. **Loops → skills** (`56edbcd`): los 24 loops se funden en las skills que
   los apuntaban (01+03 en `daily-operations`; 06 + `calibration.md` en
   `review-calibration`); `backtest/refactor/release`, sin skill, pasan como
   checklist a su agente primario. En `.claude/loops/quant/` quedan solo el
   router 00 y `STATES.md`. `model-routing.json`: `loop` → `skill` (null =
   solo agentes). `sync_agent_instructions.py` refresca los bloques de
   guardarraíles en las skills portadoras (+ router) y regenera las tres
   vistas. Tests: `QUANT_SKILLS` (12) y `GENERAL_SKILLS` (7) fijan el
   conjunto; bloques idénticos; router == json == disco; ninguna skill apunta
   a un loop borrado.
3. **Agentes** (`c56ba17`): 27 → 22. Fuera los 4 especialistas de deporte
   (7 viñetas, subconjunto de las skills `quant-*`) y `performance-engineer`.
   Los 9 sin despacho que la tabla de rutas nombra se conservan: retirarlos
   exige recablear 24 rutas y `MODEL_ROUTING.md` reserva ese cambio a
   aprobación explícita. `tipster` se conserva (complemento cualitativo del
   clasificador determinista).
4. **Scripts** (`495d2e6` + corrección al cierre): `scripts/research/` con 8
   de medición y experimento. `prepare_feature_candidates.py` volvió a
   `scripts/`: la suite completa reveló que `test_feature_integration.py` lo
   carga por ruta (mi criba lo había dado por «solo docs» porque el test lo
   cita sin extensión); es un paso del protocolo de features con test de
   contrato, no un experimento. `run_backtest.py`, `backfill_*`,
   `clean_corrupt_novig` y el protocolo V1 de revisión se quedan (V2 importa de `codex_review.py`;
   `test_codex_review.py` cubre al resto).

Después: 34 skills (sin capa doble), 8 comandos, 2 ficheros en `loops/`, 22
agentes, 48 + 8 scripts, 7.663 líneas en `.claude/`. Las 4 skills de deporte
sin uso se mantienen a propósito (NFL en curso, NBA/NHL en octubre).
Validación: 585 tests de los ficheros que tocan `.claude/` + suite completa
`not slow`; `sync_agent_instructions.py --check` limpio; ruff limpio.
Pendiente de decisión del operador: el roster de 9 agentes sin despacho.
