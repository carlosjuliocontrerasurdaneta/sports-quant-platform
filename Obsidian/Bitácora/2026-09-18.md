---
tags: [bitacora, auditoria, remediacion, calibracion, gates, hooks, sqp]
creada: 2026-09-18
actualizada: 2026-09-18
---

# 2026-09-18 — Auditoría integral (ronda `audit-2026-09-18`) con dos auditores y remediación autorizada

Ronda inicializada por el coordinador a las 00:04 sobre `5952164` (`main`,
sincronizado, CI verde). **Dos auditores independientes**: OpenAI
(`audit/latest/openai/REPORT.md`, 00:17) y Claude (`claude/REPORT.md`, 08:30–
09:30), sin leerse hasta fijar conclusiones. Conjuntos disjuntos (3 + 4);
consolidados en `audit/latest/FINDINGS.md`: **7 confirmados, MEDIUM 4 / LOW 3,
P0/P1 ninguno**. Línea base: suite completa 2191 passed / 1 skipped, ruff 0,
mypy 0; estado de controles comprobado (5 tareas rc 0x0, gate 0/48, 11
pausas de degradación, 4 calibradores sin defecto estructural).

- **AUD-001 (OpenAI, MEDIUM)** `scripts/gate_status.py` evaluaba OTRA regla
  (aciertos vs 0,5 sobre `pick_history`) y anunciaba «PASAN EL GATE» para
  cortes que el gate real deniega. Reescrito: veredicto persistido + progreso
  con `evaluate_markets` sobre el stream graduado.
- **AUD-002 (OpenAI, MEDIUM, ampliado)** un JSON válido con raíz no objeto
  rompía el default-deny (`load_prediction_gate`) y, en `degradation.py`, el
  fallback de `run_all` lo llamaba fuera del `try`: abortaba el run entero.
  Raíz comprobada en ambos lectores; fallback que nunca lanza.
- **AUD-003 (Claude, MEDIUM)** el registro live de calibración contenía la
  clave sandbox `mlb_h2h_pergame` (promoción completa del 23/08): inerte para
  el pipeline (`mlb_h2h` en crudo) pero contada como calibrador vivo por
  health, dashboard y [[Tareas]]. Demovida con `promote_calibration.py
  --demote` (rastro en `promotion_log.csv`); la promoción rechaza claves
  sandbox y las demueve en la sincronización completa; health/dashboard las
  ignoran y las señalan. **Decisión: demover, no adoptar** — cero cambio en lo
  servido; adoptar bajo `mlb_h2h` sigue siendo la tarea abierta.
- **AUD-004 (Claude, MEDIUM)** historial del Programador de tareas
  deshabilitado: los días sin run 11, 15 y 16/09 no tienen causa
  diagnosticable. `health_check` expone ahora `LastRunTime`/`LastTaskResult`
  de las 5 tareas y avisa (WARN) mientras el historial siga apagado.
  **Pendiente del operador (consola elevada):**
  `wevtutil sl Microsoft-Windows-TaskScheduler/Operational /e:true`.
- **AUD-005 (OpenAI, LOW)** el log del run anunciaba habilitados desde la
  tabla pre-pestillo; ahora lee el registro escrito (`gate_allowed_markets`).
- **AUD-006 (Claude, LOW)** la captura de cierre pedía 3 mercados en tenis (15
  créditos vs 5): regla única `markets_for_family`.
- **AUD-007 (Claude, LOW)** `check-secrets.sh` marcaba código citado como
  secreto en cada comando (asignación reflexiva + CRLF escapado en un
  EVIDENCE.json); excluidos `audit/`, `audits/`; `ASSIGNMENT` endurecido.

Observaciones sin acción: gate en 48/50 cortes (6 de tenis); cobertura de la
captura de cierre 0–28/48 por día (máquina apagada de noche; 55,8 % de
liquidadas sin cierre); marcador `.tests-pending` entre sesiones (subconjunto
`not slow` 489,8 s vs timeout 600 s); B-02 heredado.

Validación y estado por ID: `audit/latest/{CHANGES,VALIDATION,STATUS}.md`.
Ver [[Tareas]], [[Bitácora/2026-09-17]] y [[Conocimiento/Calibración]].
