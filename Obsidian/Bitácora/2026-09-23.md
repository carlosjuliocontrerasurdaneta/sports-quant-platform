---
tags: [bitacora, auditoria, remediacion, gate, liquidacion, calibracion, sqp]
creada: 2026-09-23
actualizada: 2026-09-23
---

# 2026-09-23 — Ronda `audit-2026-09-23`: consolidación y remediación

Primera ronda con **dos auditores independientes sobre la misma base** (`7bd565e`): Claude (5 hallazgos) y OpenAI (9). Consolidada en 14 (`AUD-001…014`: 3 HIGH, 8 MEDIUM, 3 LOW; sin P0). Detalle en `audit/latest/FINDINGS.md`, `CHANGES.md` y `STATUS.md`.

## Remediación: 13 implementados, 1 bloqueado, nada commiteado

- **Gate de predicción:**
  - se revalida *antes* de generar, y un fallo genera en default-deny (AUD-001);
  - su transacción de pestillo va bajo lock (AUD-002);
  - el centinela de bloqueo se ve en health y en `gate_status` (AUD-013).
- **Liquidación:**
  - los picks desplazados recuperan `start_time` desde `archive/` y expiran como los vigentes (AUD-004);
  - dos runs el mismo día ya no se pisan en el archivo (AUD-012).
- **Salida al operador:**
  - con banca 0 la lista se conserva a stake 0 con flag `bankroll_zero` (AUD-005);
  - `daily_picks` usa la probabilidad de decisión (AUD-006).
- **Líneas de cuarto:** las medias liquidaciones entran con peso 0,5 en el entrenamiento del calibrador (AUD-007) y con su P&L parcial en `edge_information` (AUD-008). Monte Carlo usa unidades parciales (AUD-014).
- **Datos:** los stores históricos escriben bajo lock (AUD-009). Un boxscore MLB con error ya no borra FIP (AUD-010).
- **Hook:** una revisión cruzada fallida sin salida ya no se pierde en silencio (AUD-011).

## La lección del día: AUD-003 bloqueado

El fallback de `data/historical/` para candidatos de equipo parecía la corrección obvia: el stream servido ya lo tenía. La revisión independiente en Fable lo tumbó con una reproducción (FABLE-001, CRITICAL). A la hora de liquidar, `candidates_*.csv` contiene partidos **aún no jugados**, y en una serie MLB el pick de hoy se emparejaba por (local, visitante) ± 1 día con el partido de ayer. Salía liquidado de forma **irreversible** con el marcador de otro partido.

Es el mismo problema abierto desde el 2026-09-14: la cercanía de fecha y equipos no prueba la identidad del partido. Se revirtió y queda pendiente de una decisión del operador sobre cómo identificar un evento entre proveedores. Ver [[Errores y lecciones/Errores detectados y soluciones]].

## Pendiente

- **Commit**: sin él, el guard KI-036 de `DIARIO_COMPLETO.bat` aborta el run de las 12:00.
- Verificación independiente de la ronda (`audits/prompts/verificar-remediacion.md`), y la de la ronda r2, que sigue sin hacer.
- Decisión de identidad de eventos para desbloquear AUD-003.
