# Hallazgos — Auditoría integral 2026-07-29/30

Severidades: CRÍTICO · ALTO · MEDIO · BAJO · INFORMATIVO.
Estados: **Corregido** · **Parcial** · **Requiere decisión humana** · **No corregible automáticamente** · **No confirmado**.

Prefijos: `B-` backtesting/riesgo · `Q-` modelos/calibración · `D-` datos/providers ·
`S-` seguridad/CI/ops · `K-` Claude Code/documentación.

## CRÍTICO

| ID | Componente | Evidencia | Impacto | Corrección | Prueba | Estado |
|---|---|---|---|---|---|---|
| B-01 | `audit/report.py:52,104` | `visible = d[(d["stake"] > 0) \| (flags == "shadow_mode")]` frente a `daily.py:658` que produce `f"{flag};accuracy_mode"`. Verificado en `candidates_wnba.csv`: todas las filas con `flags="shadow_mode;accuracy_mode"`. | Igualdad exacta falla → `rank_candidates` vacío → dashboard, reporte consolidado y ranking muestran 0 picks. El sistema genera picks y no los muestra. | Helper `has_flag()` con pertenencia por token en los 3 puntos. Misma clase de bug corregida en `cleanup.py:41` (`flags == ""`), latente para post-shadow. | 4 tests | **Corregido** |
| Q-01 | `pipeline/daily.py:632`, `calibration/calibrator.py:500-512` | `data/models/calibration_methods.json` contiene solo `{"mlb_totals","wnba_totals"}`. Ningún `*_h2h_calibration_*.joblib`. Con `method:"auto"` y clave ausente, `calibrate_probability` hace `return probs` (no-op). El modo precisión opera SOLO en h2h. | El umbral 0.70 se aplica al blend crudo modelo+no-vig, no a una probabilidad calibrada, y la columna se llama `calibrated_probability`. Choca con `betting-output-rules.md`. | `_warn_if_uncalibrated_accuracy()` emite WARNING por liga. **No suprime picks**: dejar de emitirlos es cambio de política productiva. | 3 tests | **Parcial** (aviso hecho; política es decisión humana) |

## ALTO

| ID | Componente | Evidencia | Impacto | Corrección | Prueba | Estado |
|---|---|---|---|---|---|---|
| Q-02 | `probabilities.py:112-114`, `default.yaml` `market_shrink: 0.5`, `daily.py:584` | `p_decision = 0.5·p_model + 0.5·fair` → el umbral equivale a `p_model + fair ≥ 1.40`. `e = edge(p_decision, price)` con `price` CON vig: si `p_decision ≈ fair` entonces `e < 0` mecánicamente. | Hit rate esperado tiende a la frecuencia implícita del mercado en la banda, no al 0.70 nominal. Picks con EV estimado negativo por construcción. | Ninguna: cambiar `market_shrink` o el significado del umbral es decisión de política sin evidencia OOS. Documentado en README. | — | **Requiere decisión humana** |
| Q-03 | `sports/adapters.py:101-105`, `models/distributions.py:100` | Medido ejecutando el adapter real: NHL Δ100 Elo → `p_model` 0.769; MLB Δ200 → 0.757. `tilt = clip((p_home-0.5)·tilt_scale, ±0.30)` da λ 4.0 vs 2.2 en NHL. | El modelo h2h está sobreconfiado; el umbral se alcanza con `fair ≈ 0.63`. Picks "≥0.70" cuya única evidencia externa los sitúa en 0.63-0.65. | Ninguna: reajustar `tilt_scale` requiere walk-forward OOS. | — | **Requiere decisión humana** |
| Q-04 | `calibrator.py:69-83` | `_no_extreme_expansion` solo prohíbe inflar hacia ≥0.95. Un mapeo `0.55 → 0.85` pasa los 4 gates. ECE es promedio ponderado sobre 10 bins: la banda ≥0.70 se diluye. | Con el modo precisión activo, un calibrador que infle la zona media-alta fabrica picks en masa, no solo edges fantasma. | Ninguna aplicada: añadir un 5º gate cambia el criterio de promoción. | — | **Requiere decisión humana** |
| Q-05 | `configs/leagues/ratings.yaml` `mlb: pitcher_bound: 0.0`, `models/starters.py:41` | `max(1.0-bound, min(1.0+bound, ...))` con `bound=0.0` devuelve 1.0 siempre. Pero `reliability_warning` sigue bloqueando el evento por `is_known()`. Docstring afirma "the largest single factor". | Las probabilidades h2h de MLB **no contienen información de abridor**, el factor dominante del moneyline de béisbol, mientras el modo precisión selecciona moneyline MLB. | Ninguna: activar `pitcher_bound` exige revalidación OOS. | — | **Requiere decisión humana** |
| B-02 | `backtesting/roi_engine.py:244-248` | `grep -rn "accuracy" src/sqp/backtesting/ src/sqp/audit/` → 1 coincidencia y es un comentario. No existe ruta que replique `_accuracy_selected`. | `validate_oos.py`, `backtest_roi.py` y `patterns.py` miden la regla por edge/Kelly que producción ya no usa. **No hay estimación OOS del hit rate del umbral 0.70.** | Ninguna aplicada (trabajo de ingeniería, no corrección). Documentado en README. | — | **Pendiente** |
| B-03 | `roi_engine.py:52-61`, `calibration/data.py:3-7` | El propio código: *"live probabilities are anchored to the opening line while the backtest is anchored to the close"*. | El backtest está mejor informado que producción → su ROI/hit rate es **optimista**. El resultado real esperado es peor que −5.32%. | Ninguna: cambiar el ancla del backtest es rediseño metodológico. | — | **Pendiente** |
| B-04 | `configs/default.yaml:11-30`, `validate_oos.py:99` | El yaml documenta que `max_plausible_edge` y `uncertainty_penalty` se eligieron maximizando ROI sobre el histórico capturado. `_freeze_on_train` congela solo `tilt_scale`/`elo_home_adv`/`dc_rho`. | El "OOS" es OOS para los parámetros de rating; para los de riesgo es in-sample (data snooping). | Ninguna: requiere re-diseño del protocolo de validación. | — | **Pendiente** |
| B-05 | `daily.py:636` vs `risk/kelly.py:19` | `kelly.py:19` (`price_decimal <= 1.0 → 0.0`) es el único filtro de cuota degenerada del pipeline; el stake plano del modo precisión no pasa por ahí. Muestra real: cuotas 1.07, 1.10, 1.16. | Cuotas corruptas `1.0` (existentes en el histórico) producían pick a stake pleno. | `_accuracy_selected` exige `price_decimal > 1.0`. | 2 tests | **Corregido** |
| B-06 | `run_all.py:128-130` → `daily.py:636` → `settle.py:60-61` | `settings.bankroll = bal` se asignaba aun con `bal < 0`. `stake = round(bal*0.02, 2) < 0`. `settle.py:61`: `"loss": -row["stake"]` → **pnl positivo en apuesta perdida**. La rama `elif stake <= 0` era inalcanzable en accuracy. | Realimenta el ledger con ganancias falsas. Enmascarado hoy por shadow; agujero post-shadow. | `max(0.0, bal)` en `run_all.py` y `if stake <= 0: continue` en la rama accuracy. | 1 test | **Corregido** |
| B-07 | `risk/degradation.py:65,120` | `p_model = _num(df, "estimated_probability")` (no `calibrated_probability`, a diferencia de `segments.py:65-72`). `roi_pause: -0.15` con histéresis que exige `roi_flat >= -0.05`. | Brier medido sobre una probabilidad distinta de la que decidió. Y el modo precisión produce ROI plano estructuralmente negativo → riesgo de auto-pausa permanente del único mercado habilitado. | Ninguna aplicada: cambiar el criterio de auto-pausa es cambio de política de riesgo. | — | **Requiere decisión humana** |
| B-08 | `config.py:97-99,269-270` | `os.getenv("SHADOW_MODE","").lower() in ("1","true","yes")` + `if "SHADOW_MODE" not in os.environ`. Con `SHADOW_MODE=` (vacío) o `=on`: la rama yaml se salta Y el default resuelve `False`. Sin log. | `shadow_mode: true` del yaml ignorado en silencio → stakes reales. Mismo patrón en `clv_gate_enabled`, `degradation_enabled`, `revalidation_enabled` y 4 más. | Helper `_env_flag()` tri-estado: valor no reconocido → `None` (cede al yaml) + WARNING. Aplicado a los 8 flags booleanos. | 5 tests | **Corregido** |
| B-13 | `probabilities.py:26` vs `audit/clv_movement.py:52` | `clv_movement` filtra `prices[prices > 1.0]`; el consenso live no. Con `price=1.0`, `implied = 1/1.0 = 1.0` es CERTEZA y `remove_vig_power` normaliza el mercado a su alrededor. | Ancla `fair` corrupta → contamina `p_used`, `adjusted_edge` y `p_decision` (la variable que decide en accuracy). | Filtro `price_decimal > 1.0` en `_consensus_lines`. | 2 tests | **Corregido** |
| D-01 | `providers/mlb_statsapi.py:61-70`, `storage/starters.py:36` | `fetch_starters` emite una fila por juego programado aunque la API no hidrate `probablePitcher` (→ `None`). `drop_duplicates(keep="last")` la deja ganar. | Cada re-corrida del backfill borraba abridores ya almacenados → degradación silenciosa de la señal dominante de MLB y evento no-apostable. | `save()` descarta filas sin ningún abridor antes del upsert, con log del conteo. | 3 tests | **Corregido** |
| D-03 | `providers/odds_api.py:79-80`, `daily.py:515-520` | TTL de caché 21600 s (6 h); el run diario construye el cliente sin `force_refresh` (a diferencia de `closing_capture.py:120`), y omite el snapshot si la respuesta vino de caché. | Picks precificados con cuotas de hasta 6 h y sin snapshot con `captured_at` real → sin ancla de entrada para CLV. Consistente con "CLV n=300 mediana 0.00". | Ninguna aplicada: cambiar el TTL o forzar refresh **aumenta el gasto de API de pago** → requiere autorización. | — | **Requiere decisión humana** |
| D-04 | `daily.py:349` | `df.to_csv(pred_path, index=False)` — la única escritura de artefacto de decisión sin atomicidad ni lock. Un truncado no lanza error: `read_csv` devuelve las filas supervivientes. | Picks vigentes desaparecen del cálculo de riesgo (`cleanup`, `revalidation`, `roi_engine`) sin aviso. | `atomic_write_csv`. | Smoke demo | **Corregido** |
| S-1 | Programador de tareas Windows | `Get-ScheduledTaskInfo`: `SQP_Diario_Completo_Cdev`, LastRunTime 2026-07-29 11:00:01, **LastTaskResult = 1**. `grep -riE 'smtp\|webhook\|telegram\|notify\|alert'` en `src/`, `scripts/`, `*.bat` → 0 resultados. | El run diario de producción falló y no existe ningún mecanismo de alerta. Los .bat propagan el código correctamente, pero no hay consumidor. | Ninguna aplicada: crear alertas o modificar tareas programadas es cambio operativo. | — | **Requiere decisión humana** |
| S-2 | 6 tareas programadas | `ExecutionTimeLimit = PT72H` + `MultipleInstances = IgnoreNew`. Para `SQP_Capture_Close_Cdev` (repetición PT30M) un proceso colgado retiene el lock hasta 3 días y descarta cada ocurrencia. | Pérdida silenciosa de hasta 144 capturas de cierre = pérdida de la base de CLV. | Ninguna aplicada: modificar el Programador requiere autorización. | — | **Requiere decisión humana** |
| K-001 | `loops/quant/01-daily-prediction.md:19` vs `README.md:76-78` | El loop ordena `RUN_DIARIO_ALL.bat`; el README declara *"la liquidación debe correr ANTES"* porque el run **sobrescribe** `candidates_*.csv`. `.claude/CLAUDE.md:50` nombraba también el batch equivocado. | Un agente que siguiera el loop destruía la cohorte pendiente de liquidación. | Loop y `CLAUDE.md` apuntan a `DIARIO_COMPLETO.bat`, con el orden crítico explícito. | — | **Corregido** |
| K-003 | Los 14 `loops/quant/*.md` | Grep: `PASS`/`DEGRADED`/`BLOCKED` aparecen 2 veces en total; `DONE` **cero**. Cinco vocabularios paralelos indefinidos (`INSUFFICIENT/MONITOR/ACTIONABLE`, `NO_DRIFT/…`, etc.). Criterios en prosa: "muestra suficiente", "evidencia insuficiente". | Ningún loop era verificable ni auditable; el criterio de salida quedaba a discreción del modelo. | `loops/quant/STATES.md`: definición exacta de los 4 estados por condiciones observables + tabla de umbrales de muestra tomados del código (no inventados). Los 14 loops la referencian. Loops 01, 03, 04 con precondiciones, comandos, artefactos y criterios propios. | — | **Corregido** |
| K-004 | 10 agentes + 5 rutas de `model-routing.json` | `model: fable` en principal-orchestrator, ml-engineer, calibration-auditor, leakage-detector, backtest-reviewer, risk-manager, sports-quant-auditor, feature-engineer, backend-architect, odds-market-auditor. Verificado en esta auditoría: 2 subagentes fallaron con *"Fable 5 uses usage credits and you're out"*. Sin fallback. | Los subagentes de mayor criticidad no arrancan. | `fable` → `opus` en 10 agentes y 5 rutas. Test que impide su reintroducción. | 2 tests | **Corregido** |
| K-005 | `skills/memoria-persistente/` | Tamaños medidos: `session-summaries.md` **20 bytes** vs `.claude/memory/` **51.526 bytes**; `project-decisions.md` 20 vs 39.427. `SKILL.md:12-20` ordenaba leer los stubs. | El agente cargaba memoria vacía y concluía "sin contexto previo" en silencio (la regla "nunca inventar memoria" convierte el fallo en ausencia de reporte). | `SKILL.md` reescrito apuntando a `.claude/memory/`; los 8 stubs convertidos en punteros explícitos. | — | **Corregido** |
| K-006 | `automation/runtime/current-task.md` | `Status: done`, `Loop: feature`, objetivo del 2026-07-14, `pytest: 407/407` (real: 466), y **"Autorización del operador: total (sesión 2026-07-14 PM), incluye commit, push"** sin caducidad. | `ORCHESTRATOR.md:15` y `route-task.md:5` hacen de este archivo la fuente del estado → un agente leía un `done` ajeno como contexto vigente, y una autorización de push de 15 días atrás como vigente. | Reseteado al estado real; autorización retirada explícitamente; regla de caducidad añadida a la skill de memoria. | — | **Corregido** |
| K-007 | `Obsidian/` | 17 archivos con nombre mojibake (doble codificación CP437/UTF-8), **verificados byte-idénticos** (sha256) a su equivalente versionado. Viola `Metodología de documentación.md:41`. | Obsidian resolvería `[[Bitácora/2026-07-28]]` de forma ambigua; una edición podía caer en la copia corrupta y perderse. | Eliminados tras verificar byte-identidad con script auditable (borrado solo si existe pareja versionada idéntica). | — | **Corregido** |
| K-017 | `README.md:61` | `pytest -q                  # 198 tests`. Real: 466. Ninguna de las tres cifras del repo coincidía entre sí. | Documentación que miente sobre el estado verificable. | Cifra eliminada en lugar de actualizada: se desactualiza en cada commit. | — | **Corregido** |

## MEDIO

| ID | Componente | Evidencia | Impacto | Corrección | Estado |
|---|---|---|---|---|---|
| Q-06 | `daily.py:568-583` | Calibración aplicada por outcome sin renormalizar el mercado. Hoy `p_decision(local)+p_decision(visita) = 1` exacto porque no hay calibrador; al promover uno, `cal(p_h)+cal(p_a) ≠ 1`. | Ambos lados del mismo partido podrían superar 0.70 → dos picks contradictorios. Latente. | Ninguna | **Requiere decisión humana** (antes de promover cualquier calibrador h2h) |
| Q-07 | `daily.py:589-599` vs `:625-637` | `adjusted_edge` y `kelly_fraction_stake` se calculan y se sobrescriben en accuracy. `low_book_penalty` y `min_books_for_consensus` solo actuaban vía Kelly. | Un h2h con `books_count = 1` genera pick a stake pleno; `fair` (50% del criterio) de un solo libro. | Ninguna: exigir `min_books` cambia qué picks se emiten. | **Requiere decisión humana** |
| Q-08 / B-14 | `revalidation.py:191-197` | `if "accuracy_mode" in str(...): continue`. Todos los picks llevan la marca → el segundo pase no evalúa ninguno. | Sin verificación de que la cuota registrada siga disponible al comienzo. La exención por edge es correcta, pero no hay sustituto. | Ninguna: añadir revalidación por umbral es feature. | **Pendiente** |
| Q-09 | `daily.py:584` vs `:664,667` | `estimated_edge` derivado de `p_decision` pero `estimated_probability` persiste `p_used`. | Recomputar el edge desde las columnas persistidas da otro número. `segments._decision_prob` sí usa la base correcta. | Ninguna (documentación de esquema) | **Pendiente** |
| Q-10 | `daily.py:613,665` | Columna `calibrated_probability` sin calibración aplicada. | Informes y KPI heredan una etiqueta falsa. | Documentado en README; renombrar rompe esquema. | **Parcial** |
| Q-11 | `default.yaml` vs `calibrator.py:420` | Comentario dice `n_val_events >= 15`; el código usa `AUTO_PROMOTE_MIN_N_VAL = 30`. | La doc miente (el comportamiento es el correcto, más estricto). | Ninguna aplicada | **Pendiente** |
| B-09 | `audit/clv.py:65,86-88` | `CLOSE_MAX_AGE_MIN=90` acota antigüedad pero no exige que el cierre sea POSTERIOR al pick. `generated_at` se arrastra y nunca se usa para ese chequeo. | Entrada y "cierre" pueden ser el mismo snapshot → `clv_pct = 0` exacto. Consistente con la mediana 0.00 con n=300 y el gate vacío. | Ninguna: cambiar el filtro altera la muestra del gate de CLV. | **Requiere decisión humana** |
| B-10 | `settle.py:67`, `html_report.py:123` y 4 más | `pnl / staked if staked else 0.0`. Bajo shadow `staked == 0` siempre. | Las 3 tarjetas del dashboard muestran ROI `0.00%` como si fuera equilibrio. No hay tarjeta de hit rate. | Documentado en loop 04 y README. | **Parcial** |
| B-11 | Todo el stack de evaluación | `bootstrap\|confidence\|wilson\|binomtest` → 0 implementaciones. `segments.py:155-159` flaguea con `\|gap\| >= 0.07` a `n >= 15`, cuando el error estándar con n=15 es ≈0.13. 4 dimensiones × liga × mercado sin corrección por comparaciones múltiples. | Se reportan métricas sin incertidumbre y se disparan gates por debajo del ruido. | Ninguna: es trabajo de ingeniería estadística. | **Pendiente** |
| B-12 | `audit/patterns.py:154-155,178-188` | `best = bm.iloc[0]` presentado en negrita como "Mercado con mayor tasa de acierto". `build_pick_history` llama `realized_roi_backtest` sin `bet_from_date` → histórico completo con parámetros sintonizados sobre él. | Argmax in-sample de docenas de comparaciones presentado como conclusión. | Ninguna | **Pendiente** |
| B-16 | `risk/bankroll.py:59-63` | `realized_pnl` suma solo `settled_*.csv`; los picks en vuelo no se descuentan. | Exposición dimensionada sobre banca parcialmente comprometida. Latente bajo shadow. | Ninguna | **Pendiente** |
| B-17 | `settlement/runner.py:25` | `DEDUP_KEY` incluye `generated_at`, que es el instante del run, no la identidad económica de la apuesta. | Dos runs el mismo día duplican stake y pnl en `settled_*.csv`, fuente única del ledger, del ROI y del entrenamiento del calibrador. | Ninguna aplicada: cambia la semántica de dedup sobre datos históricos ya liquidados. | **Requiere decisión humana** |
| B-15 | `risk/bankroll.py:97-124` | `_max_drawdown` se calcula y solo se publica. `grep kill\|halt\|stop_loss` → sin enforcement. | No hay kill switch ni límite de drawdown. | Ninguna | **Requiere decisión humana** |
| D-02 | `storage/results_store.py:14` | `KEY = ["date","home","away","game_id"]` con nombres CRUDOS; el resto del sistema usa `normalize_key`. | Un cambio de ortografía del vendor duplica el partido en el fit de Elo y en las features. | Ninguna aplicada: requiere migración del store existente y medición previa. | **Requiere decisión humana** |
| D-05 | `odds_api.py:25` | `_RETRY_STATUS` sin 429. | Un rate limit perdía el fetch completo de la liga. | 429 añadido al conjunto de reintento. | **Corregido** |
| D-06 | `odds_api.py:143-152` | `_capture_quota` solo asigna con header presente y `_get` no reseteaba `requests_last`. | En `closing_capture.py:137` una respuesta sin headers heredaba el costo anterior y lo sumaba al contador diario de créditos. | Reset por llamada. | **Corregido** (1 test) |
| D-07 | `mlb_statsapi.py:22-25,57-59,84-86` | Timeouts sí, reintentos no (a diferencia de ESPN y Odds API). | Un 5xx transitorio aborta el backfill completo. | Ninguna | **Pendiente** |
| D-08 | `daily.py:302` | `df.to_csv(f)` bajo lock pero sin tmp+replace. | Crash tras escalar stakes deja candidates truncado. | `atomic_write_csv`. | **Corregido** |
| D-09 | `features/common.py:92`, `features/mlb.py:150-151` | Artefactos de INFERENCIA (`*_team_state.csv`, `mlb_pitcher_state.csv`) sin atomicidad. | Truncado deja equipos/pitchers sin estado sin error visible. | `atomic_write_csv` en las 3. | **Corregido** |
| D-10 | `storage/feature_store.py:124-129` | El manifest hashea solo `results_<liga>.csv`. Cambiar `rolling_windows`, `ewm_span` o el código del builder no invalida la caché. | El dataset servido deja de corresponder al builder actual. | Ninguna | **Pendiente** |
| D-11 | `backfill_historical_odds.py:110,124` | Compara contra `date_iso[:10]` pero persiste con `captured_at=snap["timestamp"]`, que puede caer en otro día. | El día nunca queda registrado → la siguiente corrida re-paga ~10× créditos y duplica snapshot. | Ninguna | **Pendiente** |
| D-12 | `results_store.py:40-52` | Sin validación de que los scores sean numéricos ≥ 0 ni de `home != away`. El guard de drift de header existe para odds y served, no aquí. | Un cambio de esquema del vendor entra y falla más tarde y más lejos. | Ninguna | **Pendiente** |
| D-13 | `espn_tennis.py:78,98` | `str(c.get("id",""))` puede ser `""` y `by_id[m["game_id"]] = m` los sobrescribe mutuamente. | Todos los partidos sin id colapsan en uno, sin log. | Ninguna | **Pendiente** |
| D-14 | `configs/leagues/team_aliases.yaml` | Archivo vacío (`leagues: {}`). Un nombre no reconocido devuelve rating neutro y `reliability_warning` genérico. | Fallo cerrado pero indistinguible de un equipo genuinamente nuevo. | Ninguna | **Pendiente** |
| D-15 | `revalidation.py:63-75` | `_append_log` hace read-modify-write atómico pero sin lock. | Dos pases solapados pierden el append del más lento. | Ninguna | **Pendiente** |
| D-17 | `revalidation.py:117-121` | `pd.concat` sobre `odds_<liga>_*.csv` completo en cada pase horario. Store append-only. | Coste lineal creciente sin límite. | Ninguna | **Pendiente** |
| S-3 | Tareas programadas | `SQP_Diario_Completo` 11:00 vs `SQP_Capture_Close` desde 12:30/30min; `SQP_Backfill` lunes 09:00 vs `SQP_Refresh_ML` lunes 09:45, y `REFRESH_ML.bat:6` exige "después de un backfill reciente". Ningún .bat implementa lockfile. | Solape entre tareas distintas escribiendo los mismos artefactos. | Ninguna | **Requiere decisión humana** |
| S-4 | `logging_config.py:16-17` | `RotatingFileHandler` sobre `logs/sqp.log` sin lock inter-proceso. En Windows la rotación falla si otro proceso mantiene el handle. | Entradas perdidas o rotación que no ocurre. Interactúa con S-3. | Ninguna | **Pendiente** |
| S-5 | `.github/workflows/ci.yml` | `grep -i mypy ci.yml` → 0 resultados, pese a `[tool.mypy]` en pyproject y `mypy==2.1.0` en el lock. Verificado: pasa en verde. | La puerta estaba verde y apagada: regresiones de tipos entraban sin coste de detección. | Step `mypy src` añadido en la pata 3.12. | **Corregido** |
| S-6 | 8 `.bat` vs `ci.yml:21,75` | Producción fija `Python314`; CI prueba 3.11–3.13 en Linux y solo 3.12 en Windows. `Dockerfile` usa 3.11. | La única combinación que refleja producción (Windows + 3.14) no se prueba. | Ninguna aplicada: subir la matriz depende de disponibilidad en el runner. | **Requiere decisión humana** |
| S-7 | `Dockerfile:1-11` | Sin directiva `USER`. Sin filtración de secretos en capas (los `COPY` son explícitos). | El CMD corre como root sin necesitarlo. | `useradd` + `USER sqp`. | **Corregido** |
| S-8 | 8 `.bat`, p.ej. `RUN_DIARIO_ALL.bat:17-18` | `if not exist "%SQP_PYTHON%" set "SQP_PYTHON=python"` — el fallback reintroduce en silencio el bug que M-5 cerró. | Si Python se actualiza, el pipeline resuelve por PATH sin aviso. | Ninguna aplicada (modificar los .bat de producción). | **Requiere decisión humana** |
| S-9 | `.claude/hooks/check-secrets.sh:13` | Probado por ejecución: `ODDS_API_KEY = "abc…"` detectado; `set ODDS_API_KEY=abc…` en `.bat` **NO detectado** (exit 0). Ciego a `.env`, YAML y tokens con prefijo. | Segundo filtro con huecos, no una garantía. | 4 patrones (con comillas, sin comillas, dos puntos YAML, `sk-`/`Bearer`). Re-verificado: 5/5 positivos detectados, 3/3 legítimos sin falso positivo. | **Corregido** |
| K-002 | `loops/quant/01:19`, `03:17` | Ordenan ejecutar batches que consumen The Odds API (plan de pago) contra `ORCHESTRATOR.md:74` y `autonomy-policy.md:17`. | El loop más frecuente contradice el gate de gasto. Mitigado por `settings.json` (sin `allow` para `*.bat`). | Nota explícita de aprobación en ambos loops. | **Corregido** |
| K-009 | `ORCHESTRATOR.md:69-77`, `autonomy-policy.md:14-20` | "Desactivar shadow mode" **no aparecía literalmente** en ninguna lista de gates; solo cubierto por "production configuration changes". `11-season-transition.md:22` permite definir modo `NORMAL`/`SHADOW`. | El cambio que convierte un pick de papel en dinero real no estaba nombrado. | Añadido literalmente a ambas listas, junto a `pick_mode`, `accuracy_threshold` y `bankroll`. | **Corregido** |
| K-010 | `decision-engine.md:8-20`, `09-champion-challenger.md:20` | Ningún loop cubre "definir el gate de salida del shadow para el modo precisión", que es `Obsidian/Tareas.md:12`, el pendiente nº1. El loop 09 mide Brier/ECE/ROI/CLV, **sin hit rate ni gap por banda**. | El KPI rector no tiene loop. | Loop 04 ahora reporta hit rate y `gap` por banda como métrica principal. El gate de salida sigue sin definir. | **Parcial** |
| K-012 | `decision-engine.md:1` vs `model-routing.json` | Dos clasificadores paralelos. Matching por substring desnudo (`route-model.py:12`): `"modelo"` matchea "router de modelo"; esta auditoría se clasificó como `modeling`. `full-audit` y `security` mapean ambos a `refactor.md`. | Misrouting de la ruta más importante. Mitigado: la salida es declarada "recomendación". | Ninguna aplicada (elegir un clasificador es decisión de diseño). Test que verifica que los loops referenciados existan. | **Requiere decisión humana** |
| K-013 | `MODEL_ROUTER_INTEGRATION.json` (raíz) | `{"version":1,"changed_files":[33 rutas]}`. 0 referencias en todo el repo. Su contenido es equivalente a `git status`. | Artefacto de andamiaje en la raíz del repositorio. | Ninguna: no lo creé yo; lo señalo en lugar de borrarlo. | **Requiere decisión humana** |
| K-014 | `MODEL_ROUTING.md:12-14`, `decision-engine.md:47`, `ORCHESTRATOR.md:90-92` | La misma política escrita 3 veces, ya divergente en granularidad. | Tres copias divergirán más. | `MODEL_ROUTING.md` es la fuente única; los otros dos enlazan. | **Corregido** |
| K-023 | `.gitignore:27-29` vs disco | El `.gitignore` afirma *"superpowers-main fue eliminada deliberadamente el 2026-07-04 (**no restaurar**)"* (KI-015). En disco: **172 archivos**. Al estar ignorado, `git status` no lo muestra, así que la decisión parece respetada. | Una decisión de arquitectura registrada está revertida de facto y en silencio. | Ninguna: **no puedo confirmar** si la restauración fue intencional (¿plugin?). La contradicción es lo inaceptable. | **Requiere decisión humana** |
| K-024 | `Obsidian/Bitácora/` | Última entrada 2026-07-28; el router de modelo (33 archivos), los 14 quant loops y 25 agentes modificados están sin documentar y untracked, contra `.claude/CLAUDE.md:37`. | Trabajo relevante sin trazabilidad. | Entrada `2026-07-30.md` creada por esta auditoría. | **Corregido** |

## BAJO / INFORMATIVO

| ID | Componente | Evidencia | Corrección | Estado |
|---|---|---|---|---|
| Q-12 | `calibrator.py:219-221` | `best_method` se elige por ECE sobre el MISMO `val_df` cuyo ECE se reporta como OOS y alimenta el gate. Sin conjunto test. Optimismo de selección reconocido en el propio código y mitigado subiendo `min_n_val`, no eliminado. | Ninguna | **Pendiente** |
| Q-13 | `models/ml_predict.py:35` | Ruta ML entrenada pero desconectada; `apply_calibration(p, sport=sport)` busca `mlb_calibration_iso.joblib`, que no existe (el registro usa `<liga>_<mercado>`) → no-op silencioso. Artefactos huérfanos en `data/models/`. | Documentado en README (ya estaba). | **Pendiente** |
| Q-14 | `distributions.py:100` | NHL reparte la masa de empate 50/50; el favorito gana algo más del 50% en OT/penales. Sesga hacia el underdog (atenúa Q-03, no lo compensa). | Ninguna | **Pendiente** |
| Q-15 | `calibrator.py:381-413` | Existe staging, promoción, auto-promoción con gates y `promotion_log.csv`. **No existe** champion-vs-challenger sobre holdout común (el gate compara contra *raw*, no contra el live) ni rollback (`promote_calibrators` sobrescribe con `shutil.copyfile` sin respaldo). | Ninguna | **Pendiente** |
| Q-17 | `distributions.py:19-43` | `p_model` del adapter sin clip; `normal_margin_probs` puede devolver 1.0 por underflow. Contenido hoy por casualidad (`kelly.py` rechaza `p >= 1.0`), no por diseño. | Ninguna | **Pendiente** |
| Q-18 | `config.py:231-239` | `validate()` comprueba el rango de `accuracy_threshold` pero no que `calibration_enabled` sea True ni que exista calibrador. | Cubierto parcialmente por el WARNING de Q-01. | **Parcial** |
| Q-19 | `probabilities.py:20-27` | La no-vig se calcula sobre medianas POR OUTCOME: los dos lados pueden venir de libros distintos, y el overround del par sintético no corresponde a ningún libro. Magnitud no medida. | Ninguna | **No confirmado** (magnitud) |
| B-18 | `validate_oos.py:62-73` | `_tune_tilt` toma el argmin crudo del grid sobre train, sin gate de muestra ni holdout (a diferencia de `elo_home_adv`/`dc_rho`, que sí pasan por rolling-origin). | Ninguna | **Pendiente** |
| B-19 | `clv.py:61,79` | CLV restringido a `win\|loss` (excluye pushes, cuyo CLV existe); `entry` sin validar `> 1.0`. | Ninguna | **Pendiente** |
| B-20 | `roi_engine.py:10` | Sesgo de supervivencia declarado: solo eventos con snapshot capturado. Y `closing_capture.py:106` gasta la captura solo en ligas con picks abiertos → los eventos mejor cubiertos son los que el sistema eligió apostar. | Ninguna | **Pendiente** |
| B-21 | `roi_engine.py:221` | Correlación entre selecciones del mismo evento no modelada. En accuracy mode el filtro a solo-h2h la vuelve latente. | Ninguna | **Pendiente** |
| D-18..D-25 | varios | `feature_store` manifest no atómico (degrada al lado seguro); `atomic.py` sin `fsync`; `date.today()` local en 5 providers mientras el pipeline usa UTC; `starter_fip.py:60` `float()` sin guard; `odds_cache` sin versión de payload; backfill de FIP re-descarga ~2400 boxscores; histórico de tenis con `home = ganador` (compensado con `neutral: True`); lock degrada a sin-lock tras 30 s con solo un warning y sin contador persistente. | Ninguna | **Pendiente** |
| S-10 | `pyproject.toml:38` | `target-version = "py310"` vs `requires-python = ">=3.11"`. | `py311`. | **Corregido** |
| S-11 | `Makefile:2` | `pip install -e ".[dev]"` sin `-c requirements.lock`, mientras CI y Docker sí lo aplican. | Lockfile añadido + targets `types` y `check`. | **Corregido** |
| S-12 | `scripts/rotate_log.cmd:9` | `powershell -Command "$l='%~1'; …"`. Hoy **no explotable**: todos los llamadores pasan literales fijos. | Ninguna | **Pendiente** |
| S-13 | `config.py:63-64` | `odds_api_key` en el `repr` por defecto. Verificado que hoy nadie loguea el objeto completo. | `repr=False`. | **Corregido** |
| S-14 | `.claude/settings.json:47-48` | `Read(./.env.*)` bloquea también `.env.example`, la plantilla pública versionada. | Ninguna: **no se aplicó deliberadamente** — relajar un deny de secretos es la dirección equivocada en una auditoría de seguridad. | **Requiere decisión humana** |
| S-15 | `calibrator.py:54`, `ml_predict.py:21` | `joblib.load` sobre rutas de f-string sin normalizar; `sport`/`name` vienen de config del operador. La separación staging/live sería eludible con un id con `..`. | Ninguna | **Pendiente** |
| K-011 | `CLAUDE.md` (raíz) | `graphify-out/wiki/index.md` no existe (la instrucción es condicional, así que no es referencia rota). `graph.json` y `GRAPH_REPORT.md` sí existen. | Ninguna | **Informativo** |
| K-015 | `scripts/validate_claude_model_routing.py` | Validaba modelo y `primary_agent`, pero no que los `loop` ni los `support_agents` existieran, ni el entitlement del modelo. | 2 tests nuevos cubren loops, support_agents y modelos no disponibles. | **Corregido** |
| K-016 | `validate_claude_model_routing.py:3` | `import json, re, sys` → E401, único error de lint del repo. `.claude/hooks/__pycache__/` sin ignorar. | Imports separados; `.gitignore` actualizado; pycache eliminado. | **Corregido** |
| K-018 | `README.md` | Sin mención a `pick_mode`, `accuracy_mode` ni "hit rate"; `:117` describía la salida como "edge ≥ mínimo y Kelly", que es lo que accuracy ya NO hace. | Sección "Modo precisión" con las 3 advertencias verificadas. | **Corregido** |
| K-019 | `agents/risk/risk-manager.md:12-19` | Enumeraba solo Kelly y umbral de edge, que no gobiernan bajo `pick_mode: accuracy`. | Umbral de probabilidad y cumplimiento por banda añadidos. | **Corregido** |
| K-020 | `.claude/CLAUDE.md:26`, `rules/betting-output-rules.md` | La separación obligatoria de métricas no incluía el hit rate, ahora la métrica rectora. | Añadido, con la advertencia de que un hit rate no es una afirmación de rentabilidad. | **Corregido** |
| K-021 | `pyproject.toml` | Duplicado de S-10. | Ver S-10. | **Corregido** |
| K-022 | `docs/` | Snapshots con fecha en el nombre (`AUDIT-2026-06-14.md`). **Buena práctica**, no un hallazgo. | — | **Informativo** |
| K-025 | `automation/backlog.md:7` | Backlog vacío (`No approved autonomous task`) mientras `Obsidian/Tareas.md` tiene 17 pendientes. `decision-engine.md:43` hace que el canal autónomo se detenga siempre. Default-deny deliberado, no un bug. | Ninguna | **Requiere decisión humana** |
| K-026 | `.claude/settings.local.json.backup-audit-20260623` | Backup de 36 días en disco (ignorado por `*.backup-*`). `Tareas.md:19` deja `M-7` abierto sobre recortar permisos amplios. | Ninguna: no lo creé yo. | **Requiere decisión humana** |

## Verificado correcto (para no volver a tocarlo)

- Matemática de cuotas: americano↔decimal, probabilidad implícita, `remove_vig_power`
  con `brentq` en [0.5, 5.0] y fallback proporcional con WARNING (`odds.py`, `vig.py`).
- `_novig_probs` rechaza mercados incompletos en vez de fabricar un fair.
- Mediana real (`statistics.median`), no mediana superior, en el consenso.
- Split temporal **por grupos de evento** en calibración: los lados complementarios
  del mismo partido nunca se separan entre train y val (`calibrator.py:194-217`).
- Target = `model_probability` pre-blend, serve-anchored: sin loop
  calibrar-sobre-calibrado y sin anclaje al cierre. Cuotas de cierre solo en
  `--source backtest`, con advertencia explícita y no por defecto.
- Walk-forward real: `adapter.observe(r)` siempre después de estimar; sin splits
  aleatorios; `results_store` ordena con `kind="stable"`.
- Settlement: push de empate en h2h 2-way, push en spread `adj == 0` y total `== line`,
  identidad normalizada, void por expiración.
- Shadow mode: **no existe ruta de código que lo eluda** — un solo `BetCandidate(`,
  `_zero_stake_flag` con precedencia correcta, la revalidación solo baja stakes,
  ambos caps solo escalan `stake > 0`.
- Escrituras atómicas correctas en 9 puntos y lock inter-proceso en los 5 read-modify-write
  calientes.
- Timeouts en las 8 llamadas HTTP; credenciales redactadas en Timeout/ConnectionError/HTTPError;
  `apiKey` excluido de la clave de caché.
- Sin secretos versionados ni en el historial (174 commits, `--diff-filter=A` sobre
  `*.env *.pem *.key *secret* *token*`). Sin `shell=True`, `os.system`, `eval`, `exec`,
  `yaml.load`. `yaml.safe_load` en todos los casos.
- Integridad referencial de la config de Claude Code: **67/67 rutas válidas**, los 5 hooks
  existen, los 24 agentes referenciados existen.
